window.__LEVEL05_GEO_LAYOUT_PRESET = [{"id":"us","owner":"us","adj":["mx","ca"],"poly":[[0.236618,0.225616],[0.237975,0.229607],[0.24275,0.230833],[0.247694,0.231833],[0.25202,0.233223],[0.257112,0.233667],[0.262077,0.237665],[0.264502,0.240905],[0.264986,0.242002],[0.266272,0.241599],[0.26697,0.243795],[0.26814,0.244474],[0.270692,0.248069],[0.27184,0.257938],[0.269722,0.264278],[0.26905,0.266802],[0.270305,0.268472],[0.274228,0.265505],[0.280724,0.261869],[0.280528,0.259611],[0.281333,0.257638],[0.286611,0.257618],[0.287847,0.25502],[0.292036,0.249997],[0.301375,0.249954],[0.302542,0.248304],[0.304708,0.244917],[0.307674,0.236401],[0.31046,0.236917],[0.311691,0.246095],[0.313987,0.251057],[0.308167,0.255667],[0.303763,0.26061],[0.303264,0.264806],[0.305333,0.267889],[0.305875,0.267095],[0.303778,0.269583],[0.300389,0.270444],[0.297565,0.270996],[0.29933,0.271558],[0.296264,0.274278],[0.294577,0.273607],[0.294549,0.275402],[0.291928,0.283669],[0.291111,0.281953],[0.290778,0.283556],[0.291509,0.286644],[0.289055,0.293239],[0.289661,0.289239],[0.287917,0.2825],[0.287974,0.288426],[0.288051,0.289345],[0.288967,0.295015],[0.289646,0.302496],[0.285007,0.308267],[0.281793,0.311881],[0.27999,0.315787],[0.275375,0.322037],[0.273638,0.329278],[0.275056,0.337889],[0.276306,0.344222],[0.277533,0.354412],[0.276719,0.359966],[0.274522,0.359993],[0.273028,0.356278],[0.270263,0.34725],[0.270417,0.341389],[0.267473,0.333686],[0.263587,0.335355],[0.261741,0.332486],[0.25686,0.331809],[0.252276,0.331578],[0.251629,0.333921],[0.252173,0.337272],[0.250613,0.337183],[0.247555,0.338064],[0.243058,0.335821],[0.23931,0.334924],[0.234444,0.340341],[0.230167,0.345389],[0.2295,0.351722],[0.230167,0.356278],[0.227111,0.355222],[0.224167,0.350889],[0.221917,0.343833],[0.219562,0.336774],[0.215333,0.334667],[0.211278,0.337389],[0.209151,0.332657],[0.206579,0.327312],[0.204146,0.323586],[0.199328,0.325877],[0.191601,0.325918],[0.181069,0.319304],[0.177802,0.31882],[0.174178,0.31641],[0.171082,0.312551],[0.169219,0.310678],[0.165645,0.308627],[0.164599,0.304684],[0.15959,0.291379],[0.158463,0.288257],[0.15593,0.279072],[0.155059,0.271433],[0.154075,0.262411],[0.155499,0.252134],[0.155334,0.23964],[0.153647,0.232309],[0.158,0.233111],[0.160167,0.236889],[0.158778,0.227778],[0.174913,0.227778],[0.186111,0.227778],[0.202639,0.227778],[0.220417,0.227778],[0.235669,0.227778],[0.236618,0.225616]]},{"id":"ru","owner":"ru","adj":["cn","ua"],"poly":[[0.797139,0.072366],[0.808548,0.073833],[0.816348,0.081512],[0.803889,0.087889],[0.813943,0.089017],[0.821022,0.090262],[0.842224,0.094604],[0.852712,0.091303],[0.856833,0.100111],[0.867371,0.100909],[0.881938,0.103624],[0.886522,0.097688],[0.917642,0.102186],[0.941661,0.106296],[0.947057,0.114237],[0.960945,0.114044],[0.974491,0.116591],[0.982344,0.112125],[1,0.116869],[0.996409,0.141473],[0.996967,0.1486],[0.997856,0.153866],[0.982445,0.157485],[0.973141,0.167323],[0.960667,0.165778],[0.953381,0.17105],[0.953311,0.179917],[0.949171,0.192857],[0.944505,0.20443],[0.935527,0.216605],[0.93176,0.192328],[0.935584,0.178711],[0.949645,0.164761],[0.953496,0.152965],[0.942506,0.156811],[0.930677,0.171417],[0.920384,0.169422],[0.904131,0.170353],[0.875351,0.195947],[0.883791,0.201361],[0.892626,0.205058],[0.890314,0.221969],[0.883944,0.242734],[0.874637,0.258899],[0.867439,0.25953],[0.862889,0.264472],[0.86469,0.254936],[0.869714,0.2492],[0.873613,0.235675],[0.868074,0.234506],[0.859438,0.22533],[0.852609,0.214701],[0.847412,0.204661],[0.83612,0.204159],[0.835384,0.211311],[0.831357,0.221428],[0.820794,0.223305],[0.813605,0.224758],[0.803896,0.22615],[0.796913,0.220698],[0.78799,0.221722],[0.780249,0.213795],[0.771738,0.216606],[0.76615,0.222347],[0.758623,0.219471],[0.746682,0.225164],[0.741193,0.223185],[0.73449,0.220492],[0.727628,0.21771],[0.716114,0.203309],[0.706624,0.202518],[0.700623,0.197907],[0.691856,0.192304],[0.681051,0.198032],[0.671389,0.205667],[0.666576,0.211331],[0.66648,0.217543],[0.657717,0.216425],[0.645358,0.212674],[0.634938,0.222918],[0.630677,0.226933],[0.633492,0.234757],[0.636392,0.242226],[0.629672,0.252171],[0.634957,0.267728],[0.631593,0.271001],[0.627157,0.266153],[0.622031,0.263584],[0.613673,0.258988],[0.607444,0.254],[0.603898,0.247753],[0.608744,0.23864],[0.606264,0.235853],[0.610821,0.232042],[0.611303,0.224439],[0.603871,0.220089],[0.598272,0.217923],[0.594839,0.21352],[0.590877,0.209786],[0.588294,0.210546],[0.587493,0.204625],[0.590016,0.202122],[0.587179,0.199127],[0.585761,0.191383],[0.581193,0.189343],[0.577139,0.181976],[0.576167,0.173752],[0.580883,0.166511],[0.5865,0.153568],[0.584569,0.143309],[0.580707,0.128087],[0.579422,0.116307],[0.589258,0.111634],[0.611923,0.122598],[0.611155,0.131855],[0.592179,0.129819],[0.597066,0.142142],[0.603172,0.142585],[0.609982,0.141551],[0.616925,0.130688],[0.623701,0.129131],[0.620702,0.119051],[0.626542,0.12463],[0.633039,0.128419],[0.649215,0.117459],[0.652017,0.121683],[0.663339,0.117329],[0.66675,0.113778],[0.680245,0.115362],[0.689346,0.115865],[0.686833,0.111507],[0.690389,0.100364],[0.701632,0.095687],[0.701306,0.105054],[0.704633,0.119956],[0.701175,0.132374],[0.706074,0.126198],[0.708155,0.116727],[0.706666,0.107601],[0.707387,0.095376],[0.709136,0.103691],[0.715491,0.098516],[0.723919,0.096762],[0.735154,0.089967],[0.74213,0.082687],[0.758057,0.079037],[0.768551,0.078247],[0.780654,0.072989],[0.79463,0.070145]]},{"id":"cn","owner":"cn","adj":["ru","in","tw"],"poly":[[0.854604,0.223554],[0.862729,0.22928],[0.868074,0.234506],[0.875073,0.230676],[0.872534,0.237709],[0.869714,0.2492],[0.863959,0.250178],[0.864291,0.2615],[0.862889,0.264472],[0.859991,0.264306],[0.856135,0.269629],[0.852414,0.267686],[0.847444,0.274612],[0.841299,0.27979],[0.836263,0.283903],[0.837158,0.279165],[0.83789,0.27252],[0.832332,0.278344],[0.827897,0.282198],[0.827944,0.288547],[0.83031,0.291953],[0.835621,0.289609],[0.839883,0.29192],[0.8364,0.296382],[0.832402,0.302168],[0.833965,0.309109],[0.836747,0.319665],[0.838589,0.328059],[0.83751,0.332539],[0.838718,0.338789],[0.83646,0.343691],[0.832182,0.356996],[0.825782,0.368753],[0.818788,0.374066],[0.81613,0.374731],[0.810677,0.380275],[0.806789,0.386994],[0.804521,0.383288],[0.801452,0.37936],[0.797343,0.378823],[0.796459,0.373365],[0.792581,0.370266],[0.787513,0.373868],[0.783807,0.375196],[0.782786,0.382365],[0.781056,0.380908],[0.778935,0.380229],[0.775669,0.37712],[0.774719,0.371429],[0.771124,0.367237],[0.774088,0.356007],[0.774119,0.347173],[0.771978,0.342578],[0.767358,0.342161],[0.766994,0.336373],[0.762683,0.337348],[0.756953,0.345017],[0.753497,0.344219],[0.750044,0.342798],[0.746706,0.348337],[0.744779,0.34513],[0.738398,0.343313],[0.733985,0.339778],[0.731492,0.336313],[0.726461,0.330985],[0.721448,0.328429],[0.71794,0.318788],[0.720025,0.316698],[0.719201,0.309323],[0.711647,0.300564],[0.708772,0.293705],[0.707861,0.288944],[0.706271,0.285519],[0.704654,0.280938],[0.705062,0.278367],[0.709633,0.274655],[0.713624,0.271853],[0.718177,0.268988],[0.722944,0.264722],[0.724628,0.260109],[0.727631,0.248239],[0.731057,0.237055],[0.738112,0.236372],[0.740552,0.230282],[0.743754,0.226127],[0.746817,0.23295],[0.752697,0.23951],[0.752626,0.248411],[0.759669,0.250136],[0.764741,0.254215],[0.767637,0.262635],[0.776433,0.263752],[0.78287,0.263806],[0.79034,0.267176],[0.794804,0.26592],[0.803454,0.263781],[0.808694,0.258851],[0.810188,0.255149],[0.810759,0.249433],[0.815178,0.251062],[0.822181,0.24596],[0.826171,0.240707],[0.832398,0.240596],[0.830185,0.234739],[0.825821,0.235013],[0.821508,0.234853],[0.822755,0.22703],[0.827442,0.224939],[0.831332,0.218984],[0.835384,0.21131],[0.833825,0.206923],[0.839572,0.203157],[0.847412,0.204661],[0.851568,0.21231],[0.853576,0.218112]]},{"id":"in","owner":"in","adj":["cn"],"poly":[[0.716215,0.302811],[0.719201,0.309323],[0.71892,0.313854],[0.720025,0.316698],[0.719934,0.319535],[0.71794,0.318788],[0.718719,0.324912],[0.721448,0.328429],[0.725309,0.332314],[0.723546,0.334834],[0.722468,0.340031],[0.725159,0.342133],[0.727778,0.344858],[0.731401,0.347975],[0.735208,0.348695],[0.73681,0.351521],[0.738957,0.35205],[0.742299,0.353345],[0.744612,0.353252],[0.74493,0.351053],[0.744564,0.347523],[0.744779,0.34513],[0.746473,0.343962],[0.746706,0.348337],[0.746766,0.34945],[0.74929,0.351559],[0.751037,0.35069],[0.753382,0.351063],[0.755649,0.350898],[0.755844,0.347485],[0.754713,0.345713],[0.756953,0.345017],[0.759482,0.340885],[0.762683,0.337348],[0.765013,0.338713],[0.766994,0.336373],[0.768296,0.339828],[0.767358,0.342161],[0.770353,0.342991],[0.770563,0.345097],[0.769589,0.346116],[0.769817,0.349535],[0.767832,0.34853],[0.764235,0.352369],[0.76432,0.355548],[0.762787,0.360208],[0.762646,0.362915],[0.761408,0.367496],[0.759237,0.36623],[0.759129,0.37198],[0.758501,0.373872],[0.758795,0.376231],[0.757424,0.377549],[0.755961,0.368736],[0.755194,0.368754],[0.75474,0.372304],[0.753219,0.369425],[0.754077,0.366263],[0.75532,0.365942],[0.756601,0.361241],[0.754999,0.360292],[0.752423,0.360374],[0.74978,0.359613],[0.749535,0.35575],[0.748209,0.355476],[0.746008,0.353075],[0.745027,0.356844],[0.747032,0.359785],[0.745295,0.361855],[0.744679,0.36388],[0.746389,0.365368],[0.745916,0.368716],[0.746879,0.372894],[0.747311,0.377468],[0.746913,0.379497],[0.745024,0.379427],[0.741599,0.38058],[0.741759,0.384759],[0.740276,0.388046],[0.736279,0.391786],[0.733169,0.398322],[0.731081,0.401827],[0.728313,0.405463],[0.728309,0.408019],[0.726924,0.409388],[0.724422,0.411378],[0.723125,0.411671],[0.722292,0.415909],[0.72287,0.423135],[0.723017,0.427743],[0.72184,0.433021],[0.721828,0.44246],[0.72039,0.442729],[0.719126,0.446966],[0.719971,0.448797],[0.717439,0.450372],[0.716503,0.45415],[0.715389,0.455747],[0.712758,0.45056],[0.711472,0.44278],[0.710407,0.437176],[0.709434,0.434549],[0.707958,0.429211],[0.707269,0.422263],[0.706788,0.418793],[0.704262,0.411163],[0.703111,0.400397],[0.70228,0.393288],[0.70229,0.386558],[0.701751,0.381356],[0.697709,0.384681],[0.695751,0.384015],[0.692123,0.377282],[0.693458,0.375273],[0.692638,0.373093],[0.68938,0.368378],[0.691229,0.364671],[0.697342,0.364686],[0.696791,0.359916],[0.69523,0.357099],[0.694914,0.352823],[0.693096,0.350328],[0.696157,0.344504],[0.699382,0.344927],[0.702288,0.339102],[0.70403,0.333464],[0.706726,0.32789],[0.706683,0.32393],[0.709052,0.320716],[0.70681,0.317973],[0.705845,0.314214],[0.704861,0.309346],[0.706223,0.306951],[0.710436,0.308306],[0.713533,0.30748],[0.716215,0.302811]]},{"id":"jp","owner":"jp","adj":[],"poly":[[0.891601,0.293655],[0.890555,0.298089],[0.891039,0.300873],[0.889592,0.304788],[0.886043,0.307402],[0.88116,0.307743],[0.877203,0.314084],[0.875336,0.31195],[0.875221,0.307797],[0.87039,0.309023],[0.867102,0.311639],[0.86385,0.311746],[0.866667,0.315833],[0.864813,0.325276],[0.863018,0.327613],[0.861673,0.325454],[0.862355,0.320447],[0.860596,0.318832],[0.859468,0.315022],[0.862094,0.31331],[0.863551,0.309818],[0.866345,0.306946],[0.868382,0.303148],[0.873912,0.301491],[0.876882,0.302627],[0.879788,0.29275],[0.881641,0.295403],[0.885716,0.289847],[0.887296,0.287689],[0.889041,0.280896],[0.888565,0.274648],[0.889738,0.271139],[0.892692,0.270119],[0.894206,0.277824],[0.894124,0.282329],[0.891554,0.287922],[0.891601,0.293655]]},{"id":"ir","owner":"ir","adj":["sa","iq"],"poly":[[0.649782,0.293339],[0.652223,0.292264],[0.654199,0.289088],[0.656057,0.289249],[0.657276,0.288214],[0.659251,0.288727],[0.662323,0.291543],[0.664541,0.29215],[0.667716,0.29707],[0.669786,0.297269],[0.67003,0.301944],[0.668898,0.308866],[0.668135,0.312909],[0.669344,0.313729],[0.668156,0.316771],[0.669066,0.321206],[0.669283,0.324733],[0.671387,0.325669],[0.671615,0.329245],[0.669095,0.334282],[0.67047,0.337204],[0.671589,0.340559],[0.674244,0.343002],[0.674321,0.347895],[0.67565,0.348794],[0.67588,0.351353],[0.671873,0.354222],[0.670826,0.360676],[0.6656,0.358999],[0.662572,0.357722],[0.659437,0.357001],[0.658252,0.350188],[0.656923,0.349204],[0.654788,0.350196],[0.651986,0.352885],[0.648592,0.351042],[0.645788,0.346773],[0.643113,0.345191],[0.641258,0.339919],[0.639208,0.332512],[0.637713,0.333413],[0.635948,0.331572],[0.634911,0.33374],[0.633374,0.33082],[0.633346,0.32786],[0.632459,0.327862],[0.632914,0.323838],[0.631485,0.319616],[0.628082,0.316571],[0.626157,0.31129],[0.626801,0.306955],[0.628199,0.305037],[0.62799,0.301792],[0.626168,0.300125],[0.624369,0.293497],[0.622849,0.289047],[0.623393,0.287326],[0.622526,0.280955],[0.624428,0.279372],[0.624869,0.281468],[0.626271,0.284033],[0.628177,0.284771],[0.629183,0.284608],[0.632459,0.280509],[0.6335,0.280099],[0.634321,0.281729],[0.633363,0.284478],[0.635095,0.287387],[0.635787,0.28711],[0.636666,0.291206],[0.639299,0.292364],[0.641229,0.295151],[0.645178,0.296109],[0.649516,0.294639],[0.649782,0.293339]]},{"id":"tw","owner":"tw","adj":["cn"],"poly":[[0.838272,0.364476],[0.836599,0.373384],[0.835409,0.377941],[0.833945,0.373251],[0.833628,0.369132],[0.835263,0.363675],[0.837486,0.35947],[0.838753,0.361124],[0.838272,0.364476]]},{"id":"ve","owner":"ve","adj":["br"],"poly":[[0.301857,0.434576],[0.301778,0.435889],[0.300147,0.436537],[0.301053,0.439059],[0.301019,0.441964],[0.299794,0.445191],[0.300845,0.449599],[0.302043,0.449238],[0.302667,0.445222],[0.301805,0.443267],[0.301665,0.439061],[0.305124,0.436803],[0.304739,0.434184],[0.305713,0.432432],[0.30671,0.436335],[0.308658,0.436426],[0.310463,0.439524],[0.310572,0.441363],[0.313066,0.441412],[0.316034,0.440841],[0.317624,0.443329],[0.319749,0.444015],[0.321307,0.44228],[0.321339,0.440881],[0.32478,0.440546],[0.328108,0.440469],[0.32575,0.44211],[0.326699,0.444732],[0.32892,0.44515],[0.331026,0.447881],[0.331469,0.452332],[0.332916,0.452207],[0.334005,0.453516],[0.331804,0.45678],[0.331561,0.458806],[0.332512,0.460867],[0.331822,0.461908],[0.330113,0.4628],[0.330168,0.465365],[0.329416,0.466894],[0.331296,0.47111],[0.331663,0.472677],[0.330648,0.474797],[0.32754,0.476877],[0.325543,0.477739],[0.324741,0.479052],[0.322532,0.477664],[0.320476,0.476953],[0.319955,0.477464],[0.321199,0.478904],[0.321087,0.482629],[0.321472,0.486128],[0.323825,0.486605],[0.323976,0.487773],[0.321991,0.489354],[0.321669,0.491706],[0.320525,0.492618],[0.318459,0.493915],[0.317921,0.495615],[0.315762,0.495975],[0.314232,0.493037],[0.313385,0.487496],[0.312647,0.485554],[0.311639,0.48433],[0.313047,0.481564],[0.312951,0.48032],[0.312162,0.47867],[0.311603,0.474978],[0.31182,0.470994],[0.31244,0.469129],[0.31294,0.466136],[0.311958,0.465182],[0.310375,0.465815],[0.308374,0.465518],[0.307251,0.466112],[0.305296,0.461331],[0.303683,0.460623],[0.300111,0.461158],[0.299449,0.45922],[0.298765,0.458757],[0.298668,0.457597],[0.298997,0.455541],[0.298778,0.453304],[0.298165,0.452082],[0.297809,0.449528],[0.296375,0.449156],[0.297146,0.445907],[0.297485,0.441943],[0.298293,0.439878],[0.299368,0.438285],[0.300072,0.435507],[0.301857,0.434576]]},{"id":"ua","owner":"ua","adj":["ru","eu_pl"],"poly":[[0.588294,0.210546],[0.589332,0.210771],[0.590033,0.209507],[0.590877,0.209786],[0.593758,0.20925],[0.595533,0.212395],[0.594839,0.21352],[0.595069,0.215244],[0.597284,0.215513],[0.598272,0.217922],[0.598211,0.219016],[0.601739,0.220969],[0.603871,0.220089],[0.605585,0.222691],[0.607208,0.222631],[0.611303,0.224439],[0.611336,0.22607],[0.610207,0.228979],[0.610821,0.232042],[0.610384,0.233895],[0.607696,0.234302],[0.606264,0.235853],[0.606176,0.238321],[0.603959,0.238765],[0.602111,0.240563],[0.59951,0.240856],[0.597118,0.242927],[0.59728,0.246382],[0.598639,0.247722],[0.601472,0.247389],[0.60093,0.249371],[0.597889,0.250333],[0.594118,0.253547],[0.592573,0.252417],[0.593186,0.249807],[0.59015,0.248181],[0.590641,0.247116],[0.5933,0.245269],[0.592496,0.243997],[0.588178,0.242593],[0.587987,0.240521],[0.585413,0.241205],[0.584382,0.244264],[0.582231,0.248371],[0.580971,0.247417],[0.579666,0.248311],[0.578427,0.247287],[0.579126,0.246684],[0.579611,0.244778],[0.580371,0.243006],[0.580175,0.242012],[0.580756,0.241568],[0.58103,0.242337],[0.582667,0.2425],[0.583402,0.242089],[0.582884,0.241526],[0.58308,0.240698],[0.58211,0.239286],[0.581709,0.236963],[0.580697,0.236054],[0.580896,0.234172],[0.579641,0.232677],[0.578499,0.232469],[0.576451,0.230738],[0.574605,0.231288],[0.573943,0.232107],[0.572771,0.232106],[0.572072,0.233405],[0.570022,0.233939],[0.569073,0.234792],[0.567783,0.233434],[0.566003,0.233413],[0.564284,0.232798],[0.563085,0.233988],[0.562891,0.232499],[0.561349,0.230987],[0.561891,0.228748],[0.562661,0.227301],[0.563268,0.227626],[0.562551,0.225129],[0.565074,0.220508],[0.566452,0.219862],[0.56675,0.218303],[0.565353,0.213453],[0.566681,0.213236],[0.568203,0.211731],[0.570355,0.211607],[0.573161,0.212043],[0.576261,0.213376],[0.578449,0.213488],[0.579493,0.21429],[0.580536,0.213322],[0.581264,0.214621],[0.58377,0.214355],[0.584875,0.214892],[0.585054,0.212096],[0.58591,0.210876],[0.588294,0.210546]]},{"id":"br","owner":"br","adj":["ve"],"poly":[[0.33993,0.667868],[0.346771,0.6549],[0.350976,0.649575],[0.349639,0.641931],[0.348808,0.63979],[0.349186,0.63345],[0.347145,0.63334],[0.345784,0.630956],[0.345006,0.624205],[0.341996,0.62379],[0.339248,0.615182],[0.339295,0.610944],[0.339789,0.605344],[0.339626,0.597514],[0.337811,0.593762],[0.332893,0.590324],[0.332636,0.583762],[0.332058,0.579744],[0.330322,0.574885],[0.327425,0.573327],[0.324454,0.57015],[0.318327,0.564257],[0.318209,0.558397],[0.31487,0.555174],[0.310977,0.559511],[0.308927,0.561313],[0.305295,0.5618],[0.304217,0.552723],[0.299486,0.555853],[0.296592,0.552568],[0.295636,0.546802],[0.295213,0.540783],[0.296889,0.536833],[0.297321,0.531896],[0.300699,0.525522],[0.303348,0.523618],[0.3071,0.508646],[0.30673,0.503056],[0.305512,0.496992],[0.307632,0.496652],[0.306098,0.49395],[0.311476,0.490597],[0.313167,0.490444],[0.314232,0.493037],[0.317921,0.495615],[0.320525,0.492618],[0.321991,0.489354],[0.323825,0.486605],[0.321087,0.482629],[0.319955,0.477464],[0.322532,0.477664],[0.325543,0.477739],[0.330648,0.474797],[0.331296,0.47111],[0.333386,0.472144],[0.333979,0.475425],[0.333846,0.479964],[0.334115,0.487502],[0.336025,0.492679],[0.337696,0.491867],[0.339831,0.490652],[0.34227,0.489646],[0.344456,0.489902],[0.344241,0.487662],[0.34564,0.486547],[0.348542,0.487156],[0.350615,0.486796],[0.351615,0.488592],[0.35401,0.486085],[0.356506,0.47691],[0.35814,0.47972],[0.361183,0.490353],[0.359169,0.498761],[0.364943,0.501308],[0.367153,0.503231],[0.375262,0.508621],[0.376162,0.514952],[0.384798,0.516178],[0.393054,0.520559],[0.398742,0.528386],[0.402124,0.530361],[0.403528,0.540796],[0.401008,0.553607],[0.395323,0.567618],[0.392573,0.572543],[0.391994,0.587039],[0.390924,0.599265],[0.389553,0.608884],[0.386265,0.621874],[0.383366,0.627611],[0.375978,0.629733],[0.370911,0.633828],[0.36529,0.643761],[0.365348,0.650977],[0.364199,0.659301],[0.359175,0.672136],[0.354844,0.679141],[0.35174,0.687602],[0.352196,0.68182],[0.34841,0.67497],[0.344519,0.671573],[0.33993,0.667868]]},{"id":"eg","owner":"eg","adj":["sa","il"],"poly":[[0.597007,0.336104],[0.596227,0.338337],[0.595629,0.342533],[0.594874,0.345426],[0.594226,0.346396],[0.5933,0.344604],[0.592047,0.342124],[0.590065,0.334161],[0.589779,0.334664],[0.59093,0.340527],[0.592635,0.346112],[0.594735,0.354765],[0.595761,0.357786],[0.596653,0.360924],[0.599146,0.367074],[0.598594,0.368042],[0.598683,0.371653],[0.601919,0.37664],[0.602406,0.377778],[0.591389,0.377778],[0.580611,0.377778],[0.569444,0.377778],[0.569444,0.357319],[0.569444,0.337563],[0.568611,0.333088],[0.569327,0.329658],[0.568897,0.327282],[0.569902,0.324616],[0.573598,0.324524],[0.576271,0.325993],[0.579029,0.327635],[0.580315,0.3285],[0.582454,0.32674],[0.583597,0.325148],[0.586047,0.32469],[0.588022,0.325391],[0.588779,0.328147],[0.589424,0.326331],[0.59165,0.327644],[0.593815,0.327959],[0.595182,0.326559],[0.597007,0.336104]]},{"id":"sa","owner":"sa","adj":["ir","eg","il","iq"],"poly":[[0.618831,0.409178],[0.618471,0.406808],[0.617633,0.405134],[0.617419,0.402918],[0.615984,0.400928],[0.614504,0.396269],[0.61372,0.391742],[0.611799,0.387919],[0.61056,0.387006],[0.608721,0.381712],[0.608399,0.377851],[0.608518,0.374557],[0.606924,0.368397],[0.605622,0.36623],[0.604121,0.365081],[0.603208,0.361897],[0.60336,0.360641],[0.602588,0.357761],[0.601777,0.356521],[0.600692,0.352388],[0.599001,0.347908],[0.597584,0.344092],[0.596201,0.344119],[0.596633,0.34107],[0.596756,0.339125],[0.5971,0.336908],[0.600192,0.337792],[0.601392,0.336082],[0.602057,0.334082],[0.604177,0.333312],[0.604634,0.331452],[0.605552,0.330508],[0.602784,0.324953],[0.608347,0.322165],[0.608876,0.321328],[0.612222,0.322833],[0.616361,0.326722],[0.624193,0.337895],[0.629358,0.338339],[0.631833,0.338875],[0.632525,0.341522],[0.634489,0.341378],[0.635577,0.346169],[0.636943,0.347438],[0.637419,0.349389],[0.639312,0.351724],[0.63948,0.354017],[0.639204,0.355867],[0.639555,0.357733],[0.640354,0.35929],[0.640724,0.361112],[0.641139,0.362474],[0.641979,0.363576],[0.642749,0.363181],[0.643276,0.365303],[0.643383,0.366588],[0.644446,0.372216],[0.652797,0.375017],[0.653357,0.373843],[0.65463,0.377778],[0.652778,0.388889],[0.644444,0.394444],[0.636435,0.396574],[0.633843,0.399074],[0.631852,0.404907],[0.630556,0.405833],[0.629861,0.403981],[0.628796,0.404259],[0.626111,0.403704],[0.625602,0.403148],[0.622396,0.403276],[0.621643,0.403778],[0.620502,0.402333],[0.619766,0.405064],[0.620051,0.407406],[0.618831,0.409178]]},{"id":"pa","owner":"pa","adj":[],"poly":[[0.283662,0.459868],[0.282736,0.458265],[0.282141,0.455266],[0.282828,0.453782],[0.282124,0.453402],[0.281605,0.451566],[0.280221,0.450022],[0.279006,0.450376],[0.278443,0.452308],[0.277321,0.453704],[0.276715,0.453898],[0.276443,0.455054],[0.277768,0.458069],[0.277009,0.458779],[0.276608,0.459602],[0.275316,0.459886],[0.274835,0.456567],[0.274473,0.457512],[0.273557,0.457186],[0.272996,0.45495],[0.271857,0.454581],[0.271136,0.453931],[0.269944,0.45394],[0.269858,0.455145],[0.269539,0.454305],[0.269686,0.453203],[0.269917,0.452076],[0.269809,0.451071],[0.270224,0.450413],[0.269647,0.449587],[0.269631,0.447351],[0.270705,0.446855],[0.271702,0.448848],[0.271646,0.450025],[0.272754,0.450274],[0.273016,0.449822],[0.27378,0.451188],[0.275146,0.450786],[0.276328,0.449383],[0.278015,0.448262],[0.278963,0.446602],[0.280497,0.446928],[0.280393,0.447475],[0.281942,0.447664],[0.283178,0.448624],[0.284085,0.450295],[0.28513,0.451831],[0.284792,0.452643],[0.285437,0.455915],[0.284914,0.457566],[0.284018,0.457168],[0.283662,0.459868]]},{"id":"mx","owner":"mx","adj":["us"],"poly":[[0.230167,0.356278],[0.229089,0.361155],[0.228603,0.365154],[0.2284,0.372597],[0.228132,0.37531],[0.228614,0.378341],[0.229475,0.38105],[0.23003,0.385359],[0.231873,0.389495],[0.232522,0.392665],[0.233609,0.3954],[0.236558,0.396874],[0.237706,0.399198],[0.240143,0.397645],[0.242261,0.397084],[0.244341,0.396086],[0.246089,0.395133],[0.247856,0.392866],[0.248518,0.389625],[0.248746,0.384958],[0.249226,0.383334],[0.251107,0.381879],[0.254045,0.380591],[0.256504,0.380784],[0.258189,0.380314],[0.258856,0.381492],[0.258761,0.384167],[0.257269,0.38747],[0.256608,0.390852],[0.25712,0.39182],[0.256704,0.394221],[0.256008,0.398557],[0.255304,0.39713],[0.254722,0.397222],[0.254194,0.397295],[0.253199,0.400649],[0.252695,0.399992],[0.252359,0.400247],[0.25238,0.401065],[0.249811,0.401004],[0.247218,0.401013],[0.247216,0.404141],[0.245961,0.404155],[0.246995,0.406008],[0.248023,0.407292],[0.248331,0.408496],[0.248781,0.408833],[0.24871,0.410725],[0.245145,0.410741],[0.243808,0.41527],[0.244202,0.416308],[0.24388,0.417611],[0.243812,0.419229],[0.240668,0.413248],[0.239236,0.411444],[0.236968,0.409995],[0.235416,0.410398],[0.233185,0.412488],[0.231785,0.413036],[0.229823,0.411572],[0.227742,0.410515],[0.225145,0.407966],[0.223063,0.407188],[0.219918,0.404605],[0.217594,0.40195],[0.216893,0.400466],[0.215339,0.400135],[0.212497,0.398376],[0.21134,0.395841],[0.208356,0.392688],[0.206964,0.389185],[0.206302,0.386477],[0.207228,0.385935],[0.206943,0.384351],[0.207581,0.38291],[0.207595,0.380988],[0.206658,0.378494],[0.206407,0.376283],[0.205476,0.373479],[0.203028,0.367957],[0.200235,0.363617],[0.198884,0.360154],[0.196499,0.357886],[0.195989,0.356528],[0.196412,0.353095],[0.194996,0.351799],[0.193356,0.349099],[0.192664,0.345223],[0.19117,0.344771],[0.189557,0.341845],[0.188255,0.339142],[0.188134,0.337406],[0.18664,0.333216],[0.185656,0.328962],[0.185698,0.326828],[0.183689,0.324624],[0.182762,0.324866],[0.181177,0.323336],[0.180731,0.325592],[0.181191,0.328258],[0.181461,0.33243],[0.182414,0.33472],[0.184475,0.338547],[0.184933,0.339855],[0.185356,0.340251],[0.185722,0.34216],[0.186216,0.342082],[0.186773,0.345665],[0.187617,0.347079],[0.188208,0.349046],[0.189954,0.351873],[0.190876,0.357041],[0.191701,0.359474],[0.192472,0.362078],[0.192625,0.365008],[0.193964,0.365191],[0.195078,0.367716],[0.196086,0.370196],[0.196018,0.371191],[0.194849,0.373232],[0.194357,0.373205],[0.193625,0.369828],[0.191807,0.366661],[0.189804,0.363975],[0.188383,0.362564],[0.188475,0.358499],[0.188054,0.355489],[0.18673,0.353767],[0.18482,0.351288],[0.184454,0.352003],[0.183753,0.350555],[0.18204,0.349211],[0.180402,0.345985],[0.180605,0.345566],[0.181749,0.345881],[0.18278,0.343806],[0.182883,0.341299],[0.180745,0.337336],[0.179115,0.335798],[0.178091,0.332329],[0.17706,0.328686],[0.175774,0.324246],[0.174645,0.319248],[0.177802,0.31882],[0.181329,0.318218],[0.181069,0.319304],[0.185264,0.322005],[0.191601,0.325918],[0.197125,0.325878],[0.199328,0.325877],[0.199333,0.323584],[0.204146,0.323586],[0.205159,0.325556],[0.206579,0.327312],[0.20823,0.329755],[0.209151,0.332657],[0.209842,0.335711],[0.211278,0.337389],[0.213583,0.339056],[0.215333,0.334667],[0.217604,0.334559],[0.219562,0.336774],[0.220956,0.340577],[0.221917,0.343833],[0.223556,0.347],[0.224167,0.350889],[0.224944,0.3535],[0.227111,0.355222],[0.229083,0.356444],[0.230167,0.356278]]},{"id":"ca","owner":"ca","adj":["us"],"poly":[[0.24848,0.113902],[0.25218,0.115229],[0.254674,0.122926],[0.260261,0.122659],[0.262439,0.111766],[0.270493,0.11301],[0.274388,0.118524],[0.27428,0.12446],[0.268487,0.131047],[0.261752,0.130232],[0.258246,0.137706],[0.254214,0.143895],[0.248045,0.14661],[0.244629,0.150916],[0.238218,0.161674],[0.236987,0.172507],[0.242321,0.178635],[0.247506,0.181752],[0.255445,0.186269],[0.260913,0.190423],[0.268443,0.193084],[0.271011,0.198432],[0.273887,0.210234],[0.280158,0.2137],[0.280211,0.199214],[0.282698,0.193686],[0.287385,0.185921],[0.285272,0.177488],[0.285176,0.167486],[0.283036,0.15378],[0.289733,0.154009],[0.294889,0.15309],[0.300897,0.158192],[0.306693,0.16077],[0.307534,0.172459],[0.312084,0.1766],[0.318763,0.167385],[0.322765,0.169763],[0.329454,0.183514],[0.332032,0.190136],[0.338958,0.194747],[0.341842,0.20122],[0.345121,0.204054],[0.343308,0.212385],[0.336737,0.216309],[0.328545,0.221775],[0.318435,0.220566],[0.313232,0.224936],[0.305684,0.234751],[0.304847,0.238966],[0.315132,0.227038],[0.321747,0.229208],[0.320004,0.238928],[0.324519,0.245894],[0.331894,0.238845],[0.333881,0.244887],[0.324292,0.251833],[0.318433,0.258082],[0.316217,0.252972],[0.316594,0.248559],[0.311691,0.246095],[0.31046,0.236917],[0.307674,0.236401],[0.304708,0.244917],[0.302542,0.248304],[0.301375,0.249954],[0.292036,0.249997],[0.287847,0.25502],[0.286611,0.257618],[0.281333,0.257638],[0.280528,0.259611],[0.280724,0.261869],[0.274228,0.265505],[0.270305,0.268472],[0.26905,0.266802],[0.269722,0.264278],[0.27184,0.257938],[0.270692,0.248069],[0.26814,0.244474],[0.26697,0.243795],[0.266272,0.241599],[0.264986,0.242002],[0.264502,0.240905],[0.262077,0.237665],[0.257112,0.233667],[0.25202,0.233223],[0.247694,0.231833],[0.24275,0.230833],[0.237975,0.229607],[0.236618,0.225616],[0.235669,0.227778],[0.220417,0.227778],[0.202639,0.227778],[0.186111,0.227778],[0.174913,0.227778],[0.158778,0.227778],[0.153027,0.222308],[0.146012,0.217608],[0.14486,0.20928],[0.140819,0.202436],[0.1374,0.19554],[0.138867,0.189357],[0.131304,0.179484],[0.127025,0.172994],[0.123678,0.167846],[0.118188,0.17275],[0.113781,0.166667],[0.108339,0.164964],[0.108372,0.112711],[0.117927,0.116722],[0.123262,0.114916],[0.130752,0.113859],[0.139459,0.110035],[0.14344,0.11104],[0.14598,0.109015],[0.154375,0.110231],[0.158164,0.113535],[0.162577,0.112235],[0.173326,0.116604],[0.17987,0.117189],[0.179709,0.122763],[0.192228,0.123299],[0.197555,0.125659],[0.197742,0.120491],[0.202917,0.118333],[0.207381,0.119104],[0.213275,0.121679],[0.222495,0.123302],[0.226226,0.119978],[0.233,0.120892],[0.234752,0.121718],[0.238242,0.116283],[0.232024,0.110612],[0.235531,0.100442],[0.242005,0.103785],[0.243314,0.112778]]},{"id":"il","owner":"il","adj":["eg","sa"],"poly":[[0.599222,0.318282],[0.598738,0.320033],[0.597733,0.319264],[0.597152,0.322963],[0.59785,0.323587],[0.59714,0.324351],[0.597021,0.325814],[0.598327,0.325061],[0.598391,0.327222],[0.597007,0.336104],[0.595182,0.326559],[0.59599,0.324729],[0.5958,0.324414],[0.596535,0.321817],[0.597098,0.317626],[0.597496,0.316219],[0.597572,0.316162],[0.598502,0.316172],[0.598758,0.315198],[0.599503,0.315125],[0.599546,0.317399],[0.599169,0.318244],[0.599222,0.318282]]},{"id":"iq","owner":"iq","adj":["ir","sa"],"poly":[[0.626168,0.300125],[0.62799,0.301792],[0.628199,0.305037],[0.626801,0.306955],[0.626157,0.31129],[0.628082,0.316571],[0.631485,0.319616],[0.632914,0.323838],[0.632459,0.327862],[0.633346,0.32786],[0.633374,0.33082],[0.634911,0.33374],[0.633263,0.333468],[0.631396,0.333005],[0.629358,0.338339],[0.624193,0.337895],[0.616361,0.326722],[0.612222,0.322833],[0.608876,0.321328],[0.607757,0.314563],[0.613906,0.308781],[0.614955,0.302065],[0.614694,0.298007],[0.616214,0.296634],[0.617638,0.293167],[0.618831,0.292304],[0.622062,0.293021],[0.623037,0.294436],[0.624369,0.293498],[0.626168,0.300125]]},{"id":"eu_fr","owner":"eu","adj":["eu_de","eu_it","eu_es"],"poly":[[0.509967,0.220117],[0.511906,0.222736],[0.513331,0.222303],[0.515761,0.224836],[0.516383,0.225319],[0.517184,0.225201],[0.518495,0.226656],[0.522498,0.227679],[0.521094,0.231483],[0.520741,0.235441],[0.519978,0.23639],[0.518713,0.235879],[0.518802,0.237291],[0.516771,0.240412],[0.516729,0.242928],[0.518056,0.242057],[0.51901,0.244494],[0.518895,0.246063],[0.519713,0.248149],[0.51875,0.249842],[0.519465,0.25414],[0.520971,0.254845],[0.520653,0.257256],[0.518137,0.260395],[0.512658,0.258891],[0.508612,0.260693],[0.508294,0.264039],[0.505074,0.264759],[0.501949,0.262246],[0.500939,0.263447],[0.495826,0.260922],[0.494718,0.258762],[0.496155,0.25543],[0.496684,0.244362],[0.493817,0.238531],[0.491769,0.23572],[0.487523,0.233584],[0.487243,0.229532],[0.490845,0.228324],[0.49551,0.229753],[0.494629,0.223465],[0.497251,0.225848],[0.503719,0.221516],[0.504553,0.216963],[0.506982,0.215842],[0.507385,0.217795],[0.508676,0.217887],[0.509967,0.220117]]},{"id":"eu_de","owner":"eu","adj":["eu_fr","eu_it","eu_pl"],"poly":[[0.527561,0.194538],[0.52761,0.196685],[0.530417,0.19798],[0.530387,0.199952],[0.533212,0.198908],[0.534773,0.197387],[0.53791,0.19958],[0.539221,0.20135],[0.53987,0.204177],[0.539096,0.20566],[0.540104,0.20764],[0.540792,0.210611],[0.540575,0.212527],[0.541714,0.216074],[0.540474,0.216654],[0.539742,0.216015],[0.539045,0.217073],[0.53705,0.218149],[0.536019,0.219533],[0.534,0.220743],[0.534487,0.222394],[0.534781,0.224737],[0.536198,0.226072],[0.537767,0.22846],[0.536787,0.231022],[0.535789,0.231727],[0.536183,0.235347],[0.535924,0.236291],[0.535058,0.235153],[0.533726,0.234983],[0.53174,0.235979],[0.52929,0.235742],[0.528895,0.237208],[0.527489,0.235666],[0.526651,0.235972],[0.523674,0.234273],[0.523104,0.23548],[0.520741,0.235441],[0.521094,0.231483],[0.522498,0.227679],[0.518495,0.226656],[0.517184,0.225201],[0.517341,0.222765],[0.516786,0.221511],[0.517102,0.217757],[0.516635,0.211935],[0.518304,0.211933],[0.519008,0.209842],[0.5197,0.204755],[0.519181,0.202877],[0.519723,0.2017],[0.522045,0.201398],[0.52256,0.202623],[0.524446,0.199885],[0.523811,0.197802],[0.523684,0.194651],[0.525783,0.195384],[0.527561,0.194538]]},{"id":"eu_it","owner":"eu","adj":["eu_fr","eu_de"],"poly":[[0.534379,0.24018],[0.538351,0.241615],[0.53805,0.244351],[0.538716,0.246717],[0.536504,0.245907],[0.534246,0.247879],[0.5344,0.250637],[0.53406,0.25222],[0.53497,0.255048],[0.537575,0.257846],[0.538972,0.262439],[0.542063,0.266916],[0.544239,0.266882],[0.544916,0.268109],[0.544137,0.269216],[0.546625,0.271224],[0.548664,0.272905],[0.551046,0.275802],[0.551334,0.27684],[0.550815,0.278829],[0.549273,0.276235],[0.54686,0.275321],[0.545691,0.278914],[0.547699,0.280974],[0.547369,0.283873],[0.546209,0.284202],[0.544725,0.288967],[0.543567,0.289395],[0.543578,0.287697],[0.544144,0.284717],[0.544748,0.28353],[0.543663,0.280311],[0.542816,0.277509],[0.541662,0.276817],[0.540842,0.274419],[0.539057,0.273409],[0.537856,0.271176],[0.5358,0.270816],[0.53363,0.268308],[0.531089,0.264692],[0.5292,0.261492],[0.528333,0.256],[0.526951,0.255354],[0.524692,0.25352],[0.523413,0.254271],[0.521808,0.256849],[0.520653,0.257256],[0.520971,0.254845],[0.519465,0.25414],[0.51875,0.249842],[0.519713,0.248149],[0.518895,0.246063],[0.51901,0.244494],[0.520205,0.245684],[0.521544,0.24542],[0.523102,0.243535],[0.523583,0.244416],[0.524906,0.244239],[0.525508,0.241999],[0.527563,0.242695],[0.528787,0.241758],[0.529008,0.23948],[0.53069,0.24027],[0.531013,0.239213],[0.533759,0.238248],[0.534379,0.24018]]},{"id":"eu_es","owner":"eu","adj":["eu_fr"],"poly":[[0.474903,0.26733],[0.475043,0.263373],[0.473909,0.260963],[0.477838,0.256954],[0.481238,0.257956],[0.484967,0.257921],[0.487923,0.25887],[0.490229,0.258578],[0.494718,0.258762],[0.495826,0.260922],[0.500939,0.263447],[0.501949,0.262246],[0.505074,0.264759],[0.508294,0.264039],[0.508443,0.267266],[0.505811,0.270966],[0.502251,0.27214],[0.502004,0.274009],[0.500296,0.277089],[0.499226,0.281611],[0.500309,0.284786],[0.498702,0.287265],[0.498102,0.290876],[0.496004,0.291983],[0.494038,0.296255],[0.490512,0.296339],[0.487864,0.296234],[0.486124,0.298196],[0.485063,0.300295],[0.483704,0.299834],[0.482676,0.297957],[0.481888,0.294762],[0.479295,0.293901],[0.479064,0.292062],[0.480093,0.289978],[0.480474,0.288468],[0.479516,0.286816],[0.480283,0.283166],[0.47917,0.279836],[0.480371,0.279378],[0.480482,0.276753],[0.480933,0.27594],[0.480969,0.271605],[0.482253,0.270101],[0.481476,0.267315],[0.479857,0.26712],[0.479382,0.267822],[0.477741,0.267828],[0.477045,0.265109],[0.475911,0.265918],[0.474903,0.26733]]},{"id":"eu_pl","owner":"eu","adj":["ua","eu_de"],"poly":[[0.541714,0.216074],[0.540575,0.212527],[0.540792,0.210611],[0.540104,0.20764],[0.539096,0.20566],[0.53987,0.204177],[0.539221,0.20135],[0.541119,0.199718],[0.545454,0.197149],[0.548952,0.195269],[0.551725,0.196208],[0.551934,0.197563],[0.554613,0.197633],[0.558034,0.198264],[0.563142,0.19818],[0.564567,0.198775],[0.565234,0.200486],[0.565354,0.202944],[0.566125,0.205057],[0.566109,0.207272],[0.564443,0.208406],[0.5653,0.21098],[0.565353,0.213453],[0.56675,0.218303],[0.566452,0.219862],[0.565074,0.220508],[0.562551,0.225129],[0.563268,0.227626],[0.562661,0.227301],[0.560022,0.225166],[0.558022,0.225951],[0.556711,0.225381],[0.55507,0.226572],[0.553669,0.224602],[0.552527,0.225356],[0.55237,0.225021],[0.551091,0.222285],[0.549026,0.22195],[0.548763,0.22021],[0.546858,0.219589],[0.546443,0.221024],[0.544934,0.219874],[0.545107,0.218346],[0.54303,0.217863],[0.541714,0.216074]]}];

(function attachLevel05Scene() {
    const RENDER_MODE = 'level0_5';
    let mapRect = null;
    let mapClipRect = null;
    let endTurnRect = null;

    const LEVEL05_FACTION_ORDER = ['us', 'ru', 'cn', 'eu'];
    const LEVEL05_LAYOUT_VERSION = 4;
    const LEVEL05_GEOJSON_URL = 'data/countries.geo.json';
    const LEVEL05_HAS_PRESET_LAYOUT = Array.isArray(window.__LEVEL05_GEO_LAYOUT_PRESET)
        && window.__LEVEL05_GEO_LAYOUT_PRESET.length >= 10;
    const LEVEL05_MINOR_FACTIONS = ['in', 'jp', 'ir', 'tw', 've', 'ua', 'br', 'eg', 'sa', 'pa', 'mx', 'ca', 'il', 'iq'];
    const LEVEL05_VALID_OWNERS = ['us', 'ru', 'cn', 'eu', 'none'].concat(LEVEL05_MINOR_FACTIONS);
    // Keep 1:1 map coverage so regions tile seamlessly without visible gaps.
    const LEVEL05_REGION_GLOBAL_SCALE = 1;
    const LEVEL05_REGION_FOCUS = { x: 0.5, y: 0.5 };
    const LEVEL05_REGION_INSET_SCALE = 1;
    const LEVEL05_REGION_OFFSET = { x: -0.03, y: 0.08 };
    const LEVEL05_MINOR_MIN_AREA = 0.010;
    const LEVEL05_MINOR_MAX_AREA = 0.030;
    const LEVEL05_OCEAN_MAX_AREA = 0.130;
    const LEVEL05_REGION_TOP_SAFE = 0;
    const LEVEL05_REGION_BOTTOM_SAFE = 0;
    const LEVEL05_FACTION_COLOR = {
        us: 'rgba(59,130,246,0.40)',
        ru: 'rgba(239,68,68,0.40)',
        cn: 'rgba(245,158,11,0.40)',
        eu: 'rgba(168,85,247,0.40)',
        in: 'rgba(244,114,182,0.34)',
        jp: 'rgba(248,113,113,0.34)',
        ir: 'rgba(20,184,166,0.34)',
        tw: 'rgba(251,146,60,0.34)',
        ve: 'rgba(250,204,21,0.34)',
        ua: 'rgba(56,189,248,0.34)',
        br: 'rgba(74,222,128,0.34)',
        eg: 'rgba(251,191,36,0.34)',
        sa: 'rgba(163,230,53,0.34)',
        pa: 'rgba(248,113,113,0.34)',
        mx: 'rgba(110,231,183,0.34)',
        ca: 'rgba(252,165,165,0.34)',
        il: 'rgba(147,197,253,0.34)',
        iq: 'rgba(134,239,172,0.34)',
        none: 'rgba(15,23,42,0.10)'
    };
    const LEVEL05_REGION_BLUEPRINT = [
        { id: 'na_w', owner: 'us', adj: ['na_e', 'pac', 'ca', 'mx', 'pac_w'], seed: [0.12, 0.31], weight: 0.056 },
        { id: 'na_e', owner: 'us', adj: ['na_w', 'atl', 'eu_w', 've', 'ca', 'mx', 'atl_o'], seed: [0.26, 0.31], weight: 0.054 },
        { id: 'pac', owner: 'us', adj: ['na_w', 'cn_s', 'sea', 've', 'mx', 'pa', 'pac_w', 'pac_e'], seed: [0.24, 0.56], weight: 0.056 },
        { id: 'atl', owner: 'eu', adj: ['na_e', 'eu_w', 'af_n', 've', 'br', 'pa', 'atl_o'], seed: [0.41, 0.36], weight: 0.050 },
        { id: 'eu_w', owner: 'eu', adj: ['atl', 'eu_e', 'ru_w', 'af_n', 'eg', 'atl_o'], seed: [0.51, 0.31], weight: 0.048 },
        { id: 'eu_e', owner: 'eu', adj: ['eu_w', 'ru_w', 'ru_e', 'me', 'ua', 'ir'], seed: [0.61, 0.31], weight: 0.048 },
        { id: 'ru_w', owner: 'ru', adj: ['eu_w', 'eu_e', 'ru_e', 'ua', 'ir'], seed: [0.69, 0.21], weight: 0.056 },
        { id: 'ru_e', owner: 'ru', adj: ['ru_w', 'cn_n', 'cn_s', 'ua', 'jp', 'pac_e'], seed: [0.83, 0.24], weight: 0.058 },
        { id: 'cn_n', owner: 'cn', adj: ['ru_e', 'cn_s', 'me', 'jp', 'in', 'ir', 'sa', 'tw'], seed: [0.73, 0.43], weight: 0.052 },
        { id: 'cn_s', owner: 'cn', adj: ['cn_n', 'sea', 'pac', 'ru_e', 'in', 'tw', 'jp', 'pac_e'], seed: [0.71, 0.58], weight: 0.052 },
        { id: 'me', owner: 'ru', adj: ['eu_e', 'cn_n', 'af_n', 'af_s', 'ir', 'eg', 'sa', 'in', 'il', 'iq', 'ind_o'], seed: [0.58, 0.45], weight: 0.046 },
        { id: 'af_n', owner: 'eu', adj: ['atl', 'eu_w', 'me', 'af_s', 'eg', 'br', 'atl_o'], seed: [0.46, 0.50], weight: 0.048 },
        { id: 'af_s', owner: 'cn', adj: ['af_n', 'me', 'sea', 'sa', 'eg', 'br', 'ind_o'], seed: [0.52, 0.67], weight: 0.050 },
        { id: 'sea', owner: 'cn', adj: ['cn_s', 'af_s', 'pac', 'in', 'tw', 'pac_w', 'pac_e', 'ind_o'], seed: [0.61, 0.74], weight: 0.048 },
        { id: 've', owner: 've', adj: ['na_e', 'atl', 'pac', 'br', 'mx', 'pa', 'atl_o'], seed: [0.30, 0.49], weight: 0.040 },
        { id: 'br', owner: 'br', adj: ['ve', 'atl', 'af_n', 'af_s', 'atl_o'], seed: [0.36, 0.68], weight: 0.041 },
        { id: 'ua', owner: 'ua', adj: ['eu_e', 'ru_w', 'ru_e', 'ir'], seed: [0.66, 0.29], weight: 0.040 },
        { id: 'ir', owner: 'ir', adj: ['ua', 'eu_e', 'ru_w', 'me', 'cn_n', 'sa', 'iq', 'il'], seed: [0.60, 0.41], weight: 0.040 },
        { id: 'eg', owner: 'eg', adj: ['eu_w', 'af_n', 'af_s', 'me', 'sa', 'il'], seed: [0.49, 0.55], weight: 0.040 },
        { id: 'sa', owner: 'sa', adj: ['eg', 'me', 'af_s', 'cn_n', 'in', 'ir', 'iq', 'il', 'ind_o'], seed: [0.59, 0.58], weight: 0.040 },
        { id: 'in', owner: 'in', adj: ['sa', 'me', 'cn_n', 'cn_s', 'sea', 'tw', 'ind_o'], seed: [0.67, 0.64], weight: 0.041 },
        { id: 'tw', owner: 'tw', adj: ['cn_s', 'cn_n', 'sea', 'jp', 'in'], seed: [0.77, 0.56], weight: 0.038 },
        { id: 'jp', owner: 'jp', adj: ['ru_e', 'cn_n', 'cn_s', 'tw', 'pac_e'], seed: [0.89, 0.46], weight: 0.041 },
        { id: 'ca', owner: 'ca', adj: ['na_w', 'na_e', 'mx'], seed: [0.19, 0.20], weight: 0.041 },
        { id: 'mx', owner: 'mx', adj: ['na_w', 'na_e', 'pac', 've', 'pa', 'ca', 'pac_w'], seed: [0.22, 0.44], weight: 0.041 },
        { id: 'pa', owner: 'pa', adj: ['mx', 've', 'atl', 'pac'], seed: [0.30, 0.53], weight: 0.038 },
        { id: 'il', owner: 'il', adj: ['eg', 'sa', 'ir', 'iq', 'me'], seed: [0.56, 0.48], weight: 0.038 },
        { id: 'iq', owner: 'iq', adj: ['il', 'ir', 'sa', 'me', 'cn_n'], seed: [0.62, 0.49], weight: 0.039 },
        { id: 'pac_w', owner: 'none', adj: ['na_w', 'mx', 'pac', 'sea', 'pac_e'], seed: [0.02, 0.56], weight: 0.030 },
        { id: 'pac_e', owner: 'none', adj: ['ru_e', 'jp', 'cn_s', 'sea', 'pac', 'pac_w'], seed: [0.98, 0.56], weight: 0.030 },
        { id: 'atl_o', owner: 'none', adj: ['na_e', 'atl', 'eu_w', 'af_n', 've', 'br'], seed: [0.39, 0.44], weight: 0.028 },
        { id: 'ind_o', owner: 'none', adj: ['sa', 'in', 'sea', 'af_s', 'me'], seed: [0.64, 0.71], weight: 0.026 }
    ];
    const LEVEL05_GEO_TARGETS = [
        { id: 'us', owner: 'us', name: 'United States of America' },
        { id: 'ru', owner: 'ru', name: 'Russia' },
        { id: 'cn', owner: 'cn', name: 'China' },
        { id: 'in', owner: 'in', name: 'India' },
        { id: 'jp', owner: 'jp', name: 'Japan' },
        { id: 'ir', owner: 'ir', name: 'Iran' },
        { id: 'tw', owner: 'tw', name: 'Taiwan' },
        { id: 've', owner: 've', name: 'Venezuela' },
        { id: 'ua', owner: 'ua', name: 'Ukraine' },
        { id: 'br', owner: 'br', name: 'Brazil' },
        { id: 'eg', owner: 'eg', name: 'Egypt' },
        { id: 'sa', owner: 'sa', name: 'Saudi Arabia' },
        { id: 'pa', owner: 'pa', name: 'Panama' },
        { id: 'mx', owner: 'mx', name: 'Mexico' },
        { id: 'ca', owner: 'ca', name: 'Canada' },
        { id: 'il', owner: 'il', name: 'Israel' },
        { id: 'iq', owner: 'iq', name: 'Iraq' },
        { id: 'eu_fr', owner: 'eu', name: 'France' },
        { id: 'eu_de', owner: 'eu', name: 'Germany' },
        { id: 'eu_it', owner: 'eu', name: 'Italy' },
        { id: 'eu_es', owner: 'eu', name: 'Spain' },
        { id: 'eu_pl', owner: 'eu', name: 'Poland' }
    ];
    let LEVEL05_REGION_LAYOUT = Array.isArray(window.__LEVEL05_GEO_LAYOUT_PRESET)
        ? window.__LEVEL05_GEO_LAYOUT_PRESET.map(item => ({
            id: item.id,
            owner: item.owner,
            adj: Array.isArray(item.adj) ? item.adj.slice() : [],
            poly: Array.isArray(item.poly) ? item.poly.map(p => [Number(p[0]) || 0, Number(p[1]) || 0]) : []
        }))
        : buildRegionLayoutFromSeeds(LEVEL05_REGION_BLUEPRINT);
    const LEVEL05_START_OWNER = {
        na_e: 'us',
        ru_w: 'ru',
        cn_n: 'cn',
        eu_w: 'eu',
        in: 'in',
        jp: 'jp',
        ir: 'ir',
        tw: 'tw',
        ve: 've',
        ua: 'ua',
        br: 'br',
        eg: 'eg',
        sa: 'sa',
        pa: 'pa',
        mx: 'mx',
        ca: 'ca',
        il: 'il',
        iq: 'iq',
        us: 'us',
        ru: 'ru',
        cn: 'cn',
        eu_fr: 'eu',
        eu_de: 'eu',
        eu_it: 'eu',
        eu_es: 'eu',
        eu_pl: 'eu'
    };

    function isMinorFaction(faction) {
        return LEVEL05_MINOR_FACTIONS.includes(faction);
    }

    function buildInitialRegionState(regionId, fallbackOwner) {
        const owner = LEVEL05_START_OWNER[regionId] || fallbackOwner || 'none';
        const minor = isMinorFaction(owner);
        return {
            owner,
            control: owner === 'none' ? (18 + Math.random() * 14) : (minor ? (46 + Math.random() * 20) : (72 + Math.random() * 20)),
            supply: owner === 'none' ? (24 + Math.random() * 18) : (minor ? (38 + Math.random() * 20) : (65 + Math.random() * 28)),
            troops: owner === 'none' ? 0 : (minor ? (3 + ((Math.random() * 4) | 0)) : (9 + ((Math.random() * 4) | 0))),
            ap: owner === 'none' || minor ? 0 : 2
        };
    }

    function buildInitialRegionMap() {
        const regions = {};
        LEVEL05_REGION_LAYOUT.forEach(item => {
            regions[item.id] = buildInitialRegionState(item.id, item.owner);
        });
        return regions;
    }

    function normalizeCountryName(name) {
        return String(name || '').trim().toLowerCase();
    }

    function projectLonLatToNormalized(lon, lat) {
        const x = Math.max(0, Math.min(1, (Number(lon) + 180) / 360));
        const y = Math.max(0, Math.min(1, (90 - Number(lat)) / 180));
        return [x, y];
    }

    function simplifyRing(ring, maxPoints) {
        if (!Array.isArray(ring) || ring.length <= maxPoints) return ring || [];
        const out = [];
        const step = Math.max(1, Math.ceil(ring.length / maxPoints));
        for (let i = 0; i < ring.length; i += step) out.push(ring[i]);
        if (out.length < 3) return ring;
        return out;
    }

    function getLargestProjectedRing(geometry) {
        if (!geometry || !geometry.type || !Array.isArray(geometry.coordinates)) return null;
        const candidateRings = [];
        if (geometry.type === 'Polygon') {
            if (Array.isArray(geometry.coordinates[0])) candidateRings.push(geometry.coordinates[0]);
        } else if (geometry.type === 'MultiPolygon') {
            geometry.coordinates.forEach(poly => {
                if (Array.isArray(poly) && Array.isArray(poly[0])) candidateRings.push(poly[0]);
            });
        }
        if (!candidateRings.length) return null;
        let best = null;
        let bestArea = -1;
        candidateRings.forEach(ring => {
            const projected = (ring || []).map(p => projectLonLatToNormalized(p[0], p[1]));
            const reduced = simplifyRing(projected, 220);
            const area = polygonArea(reduced);
            if (area > bestArea) {
                best = reduced;
                bestArea = area;
            }
        });
        return best;
    }

    function getPolyBounds(poly) {
        if (!Array.isArray(poly) || !poly.length) return null;
        let minX = Infinity;
        let maxX = -Infinity;
        let minY = Infinity;
        let maxY = -Infinity;
        poly.forEach(p => {
            const x = Number(p[0]) || 0;
            const y = Number(p[1]) || 0;
            if (x < minX) minX = x;
            if (x > maxX) maxX = x;
            if (y < minY) minY = y;
            if (y > maxY) maxY = y;
        });
        return { minX, maxX, minY, maxY };
    }

    function boxesNearby(a, b, pad) {
        return !(a.maxX + pad < b.minX || b.maxX + pad < a.minX || a.maxY + pad < b.minY || b.maxY + pad < a.minY);
    }

    function orientation(ax, ay, bx, by, cx, cy) {
        const v = (bx - ax) * (cy - ay) - (by - ay) * (cx - ax);
        if (Math.abs(v) < 1e-9) return 0;
        return v > 0 ? 1 : -1;
    }

    function segmentsIntersect(a, b, c, d) {
        const o1 = orientation(a[0], a[1], b[0], b[1], c[0], c[1]);
        const o2 = orientation(a[0], a[1], b[0], b[1], d[0], d[1]);
        const o3 = orientation(c[0], c[1], d[0], d[1], a[0], a[1]);
        const o4 = orientation(c[0], c[1], d[0], d[1], b[0], b[1]);
        return o1 !== o2 && o3 !== o4;
    }

    function sqDistPointToSegment(p, a, b) {
        const vx = b[0] - a[0];
        const vy = b[1] - a[1];
        const wx = p[0] - a[0];
        const wy = p[1] - a[1];
        const c1 = vx * wx + vy * wy;
        if (c1 <= 0) {
            const dx = p[0] - a[0];
            const dy = p[1] - a[1];
            return dx * dx + dy * dy;
        }
        const c2 = vx * vx + vy * vy;
        if (c2 <= c1) {
            const dx = p[0] - b[0];
            const dy = p[1] - b[1];
            return dx * dx + dy * dy;
        }
        const t = c1 / c2;
        const px = a[0] + vx * t;
        const py = a[1] + vy * t;
        const dx = p[0] - px;
        const dy = p[1] - py;
        return dx * dx + dy * dy;
    }

    function polyNearOrTouch(polyA, polyB, threshold) {
        if (!Array.isArray(polyA) || !Array.isArray(polyB) || polyA.length < 3 || polyB.length < 3) return false;
        for (let i = 0; i < polyA.length; i++) {
            const a1 = polyA[i];
            const a2 = polyA[(i + 1) % polyA.length];
            for (let j = 0; j < polyB.length; j++) {
                const b1 = polyB[j];
                const b2 = polyB[(j + 1) % polyB.length];
                if (segmentsIntersect(a1, a2, b1, b2)) return true;
            }
        }
        const t2 = threshold * threshold;
        for (let i = 0; i < polyA.length; i++) {
            const p = polyA[i];
            for (let j = 0; j < polyB.length; j++) {
                const b1 = polyB[j];
                const b2 = polyB[(j + 1) % polyB.length];
                if (sqDistPointToSegment(p, b1, b2) <= t2) return true;
            }
        }
        return false;
    }

    function buildAdjacencyForLayout(layout) {
        const pad = 0.012;
        const near = 0.008;
        layout.forEach(item => { item.adj = []; item._bbox = getPolyBounds(item.poly); });
        for (let i = 0; i < layout.length; i++) {
            for (let j = i + 1; j < layout.length; j++) {
                const a = layout[i];
                const b = layout[j];
                if (!a._bbox || !b._bbox) continue;
                if (!boxesNearby(a._bbox, b._bbox, pad)) continue;
                if (polyNearOrTouch(a.poly, b.poly, near)) {
                    a.adj.push(b.id);
                    b.adj.push(a.id);
                }
            }
        }
        layout.forEach(item => { delete item._bbox; });
        return layout;
    }

    function buildRegionLayoutFromGeoJson(geojson) {
        if (!geojson || !Array.isArray(geojson.features)) return [];
        const featureByName = {};
        geojson.features.forEach(f => {
            const n = normalizeCountryName(f?.properties?.name);
            if (n) featureByName[n] = f;
        });

        const layout = [];
        LEVEL05_GEO_TARGETS.forEach(target => {
            const feature = featureByName[normalizeCountryName(target.name)];
            if (!feature) return;
            const ring = getLargestProjectedRing(feature.geometry);
            if (!Array.isArray(ring) || ring.length < 3) return;
            layout.push({
                id: target.id,
                owner: target.owner,
                adj: [],
                poly: ring
            });
        });
        if (layout.length < 10) return [];
        return buildAdjacencyForLayout(layout);
    }

    async function tryLoadGeoJsonLayout() {
        if (LEVEL05_HAS_PRESET_LAYOUT) return;
        if (window.location && window.location.protocol === 'file:') return;
        try {
            const res = typeof window.__fetchWorldAsset === 'function'
                ? await window.__fetchWorldAsset(LEVEL05_GEOJSON_URL, { cache: 'no-store' })
                : await fetch(LEVEL05_GEOJSON_URL, { cache: 'no-store' });
            if (!res || !res.ok) return;
            const geojson = await res.json();
            const geoLayout = buildRegionLayoutFromGeoJson(geojson);
            if (!Array.isArray(geoLayout) || geoLayout.length < 10) return;
            LEVEL05_REGION_LAYOUT = geoLayout;
            if (window.game && window.game.level05War) {
                window.game.level05War.layoutVersion = 0;
            }
        } catch (_) {}
    }

    function polygonArea(poly) {
        if (!Array.isArray(poly) || poly.length < 3) return 0;
        let area = 0;
        for (let i = 0; i < poly.length; i++) {
            const p1 = poly[i];
            const p2 = poly[(i + 1) % poly.length];
            area += (Number(p1[0]) || 0) * (Number(p2[1]) || 0) - (Number(p2[0]) || 0) * (Number(p1[1]) || 0);
        }
        return Math.abs(area) * 0.5;
    }

    function clampWeight(w, owner) {
        const ww = Number(w) || 0;
        if (owner === 'none') return Math.max(0.008, Math.min(0.07, ww));
        if (isMinorFaction(owner)) return Math.max(0.018, Math.min(0.07, ww));
        return Math.max(0.03, Math.min(0.08, ww));
    }

    function buildRegionLayoutRaw(blueprint) {
        if (!Array.isArray(blueprint)) return [];
        return blueprint.map((item, i) => ({
            id: item.id,
            owner: item.owner,
            adj: Array.isArray(item.adj) ? item.adj.slice() : [],
            poly: buildVoronoiCell(blueprint, i)
        }));
    }

    function buildRegionLayoutFromSeeds(blueprint) {
        if (!Array.isArray(blueprint)) return [];
        const tuned = blueprint.map(item => ({
            ...item,
            weight: clampWeight(item.weight, item.owner)
        }));

        let layout = buildRegionLayoutRaw(tuned);
        for (let iter = 0; iter < 18; iter++) {
            let changed = false;
            const areaMap = {};
            layout.forEach(item => {
                areaMap[item.id] = polygonArea(item.poly);
            });

            tuned.forEach(item => {
                const owner = item.owner;
                const area = areaMap[item.id] || 0;
                if (isMinorFaction(owner)) {
                    if (area < LEVEL05_MINOR_MIN_AREA) {
                        item.weight = clampWeight((Number(item.weight) || 0) + 0.004, owner);
                        changed = true;
                    } else if (area > LEVEL05_MINOR_MAX_AREA) {
                        item.weight = clampWeight((Number(item.weight) || 0) - 0.002, owner);
                        changed = true;
                    }
                } else if (owner === 'none') {
                    if (area > LEVEL05_OCEAN_MAX_AREA) {
                        item.weight = clampWeight((Number(item.weight) || 0) - 0.0025, owner);
                        changed = true;
                    }
                }
            });

            if (!changed) break;
            layout = buildRegionLayoutRaw(tuned);
        }
        return layout;
    }

    function buildVoronoiCell(blueprint, index) {
        const site = blueprint[index]?.seed || [0.5, 0.5];
        let cell = [[0, 0], [1, 0], [1, 1], [0, 1]];
        for (let j = 0; j < blueprint.length; j++) {
            if (j === index) continue;
            const other = blueprint[j]?.seed;
            if (!Array.isArray(other)) continue;
            const a = 2 * ((other[0] || 0) - (site[0] || 0));
            const b = 2 * ((other[1] || 0) - (site[1] || 0));
            const siteW = Number(blueprint[index]?.weight) || 0;
            const otherW = Number(blueprint[j]?.weight) || 0;
            const c = (other[0] || 0) * (other[0] || 0)
                + (other[1] || 0) * (other[1] || 0)
                - otherW
                - (site[0] || 0) * (site[0] || 0)
                - (site[1] || 0) * (site[1] || 0)
                + siteW;
            cell = clipPolyHalfPlane(cell, a, b, c);
            if (!cell.length) break;
        }
        return cell;
    }

    function clipPolyHalfPlane(poly, a, b, c) {
        const eps = 1e-9;
        const inside = p => (a * p[0] + b * p[1]) <= c + eps;
        const intersect = (p1, p2) => {
            const v1 = a * p1[0] + b * p1[1] - c;
            const v2 = a * p2[0] + b * p2[1] - c;
            const t = v1 / (v1 - v2 || eps);
            return [
                p1[0] + (p2[0] - p1[0]) * t,
                p1[1] + (p2[1] - p1[1]) * t
            ];
        };

        const out = [];
        for (let i = 0; i < poly.length; i++) {
            const cur = poly[i];
            const prev = poly[(i + poly.length - 1) % poly.length];
            const curIn = inside(cur);
            const prevIn = inside(prev);
            if (curIn) {
                if (!prevIn) out.push(intersect(prev, cur));
                out.push(cur);
            } else if (prevIn) {
                out.push(intersect(prev, cur));
            }
        }
        return out;
    }

    function isRenderMode(mode) {
        return mode === RENDER_MODE;
    }

    function resetFrameState() {
        mapRect = null;
        mapClipRect = null;
        endTurnRect = null;
    }

    function getFactionPowerScale(faction) {
        if (faction === 'us') return 1.06;
        if (faction === 'ru') return 1.02;
        if (faction === 'cn') return 1.08;
        if (faction === 'eu') return 1.00;
        if (isMinorFaction(faction)) return 0.86;
        return 1;
    }

    function getPlayerFactionFromGameCountry(fallback = 'cn') {
        const country = String(window?.game?.country || '').toLowerCase();
        const map = {
            usa: 'us',
            us: 'us',
            china: 'cn',
            cn: 'cn',
            eu: 'eu',
            russia: 'ru',
            ru: 'ru'
        };
        return map[country] || fallback;
    }

    function ensureWarState() {
        if (!window.game) return null;
        const prevWar = window.game.level05War;
        const shouldRebuild = !prevWar
            || !prevWar.regions
            || Number(prevWar.layoutVersion || 0) !== LEVEL05_LAYOUT_VERSION
            || Number(prevWar.layoutRegionCount || 0) !== LEVEL05_REGION_LAYOUT.length;
        if (shouldRebuild) {
            window.game.level05War = {
                playerFaction: (prevWar && prevWar.playerFaction) || 'cn',
                turn: 1,
                phase: 'player',
                selectedRegionId: null,
                lastActionAt: 0,
                hoverRegionId: null,
                hoverEndTurn: false,
                attackFx: [],
                regions: buildInitialRegionMap(),
                layoutVersion: LEVEL05_LAYOUT_VERSION,
                layoutRegionCount: LEVEL05_REGION_LAYOUT.length
            };
        }
        const war = window.game.level05War;
        const desiredFaction = getPlayerFactionFromGameCountry((prevWar && prevWar.playerFaction) || 'cn');
        war.playerFaction = desiredFaction;
        war.layoutVersion = LEVEL05_LAYOUT_VERSION;
        war.layoutRegionCount = LEVEL05_REGION_LAYOUT.length;
        // Backfill newly added regions for old saves so minor countries/oceans always render.
        if (!war.regions || typeof war.regions !== 'object') war.regions = {};
        LEVEL05_REGION_LAYOUT.forEach(item => {
            if (war.regions[item.id]) return;
            war.regions[item.id] = buildInitialRegionState(item.id, item.owner);
        });
        war.phase = war.phase === 'ai' ? 'ai' : 'player';
        war.turn = Math.max(1, Number(war.turn) || 1);
        war.selectedRegionId = typeof war.selectedRegionId === 'string' ? war.selectedRegionId : null;
        war.hoverEndTurn = !!war.hoverEndTurn;
        war.attackFx = Array.isArray(war.attackFx) ? war.attackFx : [];
        LEVEL05_REGION_LAYOUT.forEach(item => {
            const s = war.regions[item.id];
            if (!s) return;
            s.owner = LEVEL05_VALID_OWNERS.includes(s.owner) ? s.owner : (LEVEL05_START_OWNER[item.id] || 'none');
            s.control = Math.max(1, Math.min(100, Number(s.control) || 60));
            s.supply = Math.max(1, Math.min(100, Number(s.supply) || 60));
            const troopsFloor = s.owner === 'none' ? 0 : 1;
            const cap = isMinorFaction(s.owner) ? 12 : 40;
            s.troops = Math.max(troopsFloor, Math.min(cap, Math.floor(Number(s.troops) || 0)));
            s.ap = Math.max(0, Math.min(4, Math.floor(Number(s.ap) || 0)));
        });
        // For migrated saves, restore configured starting owners on early turns.
        if (war.turn <= 2) {
            LEVEL05_REGION_LAYOUT.forEach(item => {
                const startOwner = LEVEL05_START_OWNER[item.id];
                const s = war.regions[item.id];
                if (!s || !startOwner) return;
                if (s.owner === 'none') {
                    s.owner = startOwner;
                    s.control = Math.max(40, Number(s.control) || 40);
                    s.supply = Math.max(34, Number(s.supply) || 34);
                    s.troops = Math.max(2, Number(s.troops) || 2);
                }
            });
        }

        return war;
    }

    function refreshTurnAp(war) {
        if (!war) return;
        LEVEL05_REGION_LAYOUT.forEach(item => {
            const s = war.regions[item.id];
            if (!s) return;
            if (s.owner === 'none' || isMinorFaction(s.owner)) s.ap = 0;
            else s.ap = s.owner === war.playerFaction ? 2 : 1;
        });
    }

    function getFactionRegionCount(war, faction) {
        if (!war || !faction) return 0;
        let count = 0;
        LEVEL05_REGION_LAYOUT.forEach(item => {
            const s = war.regions[item.id];
            if (s && s.owner === faction) count++;
        });
        return count;
    }

    function getFactionName(faction) {
        if (faction === 'cn') return 'China';
        if (faction === 'us') return 'United States';
        if (faction === 'ru') return 'Russia';
        if (faction === 'eu') return 'European Union';
        if (faction === 'in') return 'India';
        if (faction === 'jp') return 'Japan';
        if (faction === 'ir') return 'Iran';
        if (faction === 'tw') return 'Taiwan';
        if (faction === 've') return 'Venezuela';
        if (faction === 'ua') return 'Ukraine';
        if (faction === 'br') return 'Brazil';
        if (faction === 'eg') return 'Egypt';
        if (faction === 'sa') return 'Saudi Arabia';
        if (faction === 'pa') return 'Panama';
        if (faction === 'mx') return 'Mexico';
        if (faction === 'ca') return 'Canada';
        if (faction === 'il') return 'Israel';
        if (faction === 'iq') return 'Iraq';
        return 'This nation';
    }

    function getFactionNameEn(faction) {
        if (faction === 'cn') return 'China';
        if (faction === 'us') return 'United States';
        if (faction === 'ru') return 'Russia';
        if (faction === 'eu') return 'European Union';
        if (faction === 'in') return 'India';
        if (faction === 'jp') return 'Japan';
        if (faction === 'ir') return 'Iran';
        if (faction === 'tw') return 'Taiwan';
        if (faction === 've') return 'Venezuela';
        if (faction === 'ua') return 'Ukraine';
        if (faction === 'br') return 'Brazil';
        if (faction === 'eg') return 'Egypt';
        if (faction === 'sa') return 'Saudi Arabia';
        if (faction === 'pa') return 'Panama';
        if (faction === 'mx') return 'Mexico';
        if (faction === 'ca') return 'Canada';
        if (faction === 'il') return 'Israel';
        if (faction === 'iq') return 'Iraq';
        return '';
    }

    function triggerNationDefeat(war, faction) {
        if (!war || war.gameOverShown) return;
        war.gameOverShown = true;
        war.phase = 'gameover';
        const nationName = getFactionName(faction);
        if (typeof window.showStoryEvent === 'function') {
            window.showStoryEvent(
                'Nation Defeated',
                `${nationName} has lost all regions. Campaign failed.`,
                'Restart',
                () => {
                    try {
                        localStorage.removeItem('spaceEmpireV5');
                        sessionStorage.setItem('resetFlag', 'true');
                    } catch (_) {}
                    location.reload(true);
                }
            );
        }
    }

    function checkElimination(war) {
        if (!war) return;
        if (getFactionRegionCount(war, war.playerFaction) <= 0) {
            triggerNationDefeat(war, war.playerFaction);
        }
    }

    function polygonCenter(poly) {
        if (!Array.isArray(poly) || !poly.length) return { x: 0.5, y: 0.5 };
        let sx = 0;
        let sy = 0;
        poly.forEach(p => {
            sx += Number(p[0]) || 0;
            sy += Number(p[1]) || 0;
        });
        return { x: sx / poly.length, y: sy / poly.length };
    }

    function polygonBounds(poly) {
        if (!Array.isArray(poly) || !poly.length) return { minX: 0, maxX: 1, minY: 0, maxY: 1 };
        let minX = Infinity;
        let maxX = -Infinity;
        let minY = Infinity;
        let maxY = -Infinity;
        poly.forEach(p => {
            const x = Number(p[0]) || 0;
            const y = Number(p[1]) || 0;
            if (x < minX) minX = x;
            if (x > maxX) maxX = x;
            if (y < minY) minY = y;
            if (y > maxY) maxY = y;
        });
        return { minX, maxX, minY, maxY };
    }

    function toScreenPoly(def) {
        if (!def || !mapRect || !Array.isArray(def.poly)) return null;
        const poly = getNormalizedPoly(def.poly);
        return poly.map(p => ({
            x: mapRect.x + (Number(p[0]) || 0) * mapRect.w,
            y: mapRect.y + (Number(p[1]) || 0) * mapRect.h
        }));
    }

    function getNormalizedPoly(poly) {
        if (!Array.isArray(poly)) return [];
        const s = LEVEL05_REGION_GLOBAL_SCALE;
        const fx = LEVEL05_REGION_FOCUS.x;
        const fy = LEVEL05_REGION_FOCUS.y;
        const ox = LEVEL05_REGION_OFFSET.x;
        const oy = LEVEL05_REGION_OFFSET.y;
        const verticalSpan = Math.max(0.1, 1 - LEVEL05_REGION_TOP_SAFE - LEVEL05_REGION_BOTTOM_SAFE);
        const clamp01 = v => Math.max(0, Math.min(1, v));
        const scaled = poly.map(p => {
            const x = Number(p[0]) || 0;
            const y = Number(p[1]) || 0;
            const nx = fx + (x - fx) * s;
            const ny = fy + (y - fy) * s;
            return [
                clamp01(nx + ox),
                clamp01(LEVEL05_REGION_TOP_SAFE + ny * verticalSpan + oy)
            ];
        });
        if (LEVEL05_REGION_INSET_SCALE >= 0.999) return scaled;
        const c = polygonCenter(scaled);
        const is = LEVEL05_REGION_INSET_SCALE;
        return scaled.map(p => [
            c.x + (p[0] - c.x) * is,
            c.y + (p[1] - c.y) * is
        ]);
    }

    function pointInPolygon(x, y, poly) {
        if (!Array.isArray(poly) || poly.length < 3) return false;
        let inside = false;
        for (let i = 0, j = poly.length - 1; i < poly.length; j = i++) {
            const xi = poly[i].x;
            const yi = poly[i].y;
            const xj = poly[j].x;
            const yj = poly[j].y;
            const intersect = ((yi > y) !== (yj > y))
                && (x < ((xj - xi) * (y - yi)) / ((yj - yi) || 1e-9) + xi);
            if (intersect) inside = !inside;
        }
        return inside;
    }

    function getRegionScreenPos(def) {
        if (!def || !mapRect) return null;
        const normalizedPoly = getNormalizedPoly(def.poly);
        const center = polygonCenter(normalizedPoly);
        const bounds = polygonBounds(normalizedPoly);
        const rw = Math.max(0.02, bounds.maxX - bounds.minX) * mapRect.w;
        const rh = Math.max(0.02, bounds.maxY - bounds.minY) * mapRect.h;
        return {
            x: mapRect.x + center.x * mapRect.w,
            y: mapRect.y + center.y * mapRect.h,
            r: Math.max(16, Math.min(rw, rh) * 0.34)
        };
    }

    function getRegionByPoint(x, y) {
        if (!mapRect || !mapClipRect) return null;
        if (x < mapClipRect.x || x > mapClipRect.x + mapClipRect.w || y < mapClipRect.y || y > mapClipRect.y + mapClipRect.h) {
            return null;
        }
        for (let i = LEVEL05_REGION_LAYOUT.length - 1; i >= 0; i--) {
            const def = LEVEL05_REGION_LAYOUT[i];
            const screenPoly = toScreenPoly(def);
            if (pointInPolygon(x, y, screenPoly)) return def;
        }
        return null;
    }
    function getNearestRegionByPoint(x, y) {
        if (!mapRect || !mapClipRect) return null;
        let best = null;
        let bestD2 = Infinity;
        LEVEL05_REGION_LAYOUT.forEach(def => {
            const pos = getRegionScreenPos(def);
            if (!pos) return;
            const dx = x - pos.x;
            const dy = y - pos.y;
            const d2 = dx * dx + dy * dy;
            if (d2 < bestD2) {
                bestD2 = d2;
                best = { def, r: pos.r };
            }
        });
        if (!best) return null;
        const threshold = Math.max(26, (Number(best.r) || 18) * 1.6);
        return bestD2 <= threshold * threshold ? best.def : null;
    }
    function isPointInMap(x, y) {
        if (!mapClipRect) return false;
        return x >= mapClipRect.x && x <= mapClipRect.x + mapClipRect.w
            && y >= mapClipRect.y && y <= mapClipRect.y + mapClipRect.h;
    }

    function resolveAttack(war, attackerFaction, fromDef, toDef) {
        if (!war || !fromDef || !toDef) return false;
        const fromState = war.regions[fromDef.id];
        const toState = war.regions[toDef.id];
        if (!fromState || !toState) return false;
        if (fromState.owner !== attackerFaction || toState.owner === attackerFaction) return false;
        if (!Array.isArray(fromDef.adj) || !fromDef.adj.includes(toDef.id)) return false;
        if ((Number(fromState.troops) || 0) < 2) return false;
        if ((Number(fromState.ap) || 0) <= 0) return false;

        const committed = Math.max(2, Math.min(8, Math.floor((Number(fromState.troops) || 0) * 0.55)));
        fromState.troops = Math.max(1, (Number(fromState.troops) || 0) - committed);
        fromState.ap = Math.max(0, (Number(fromState.ap) || 0) - 1);

        const attPower = getFactionPowerScale(attackerFaction)
            * (0.72 + fromState.control / 140)
            * (0.75 + fromState.supply / 160)
            * (0.75 + committed / 10);
        const defPower = getFactionPowerScale(toState.owner) * (0.62 + toState.control / 150) * (0.78 + toState.supply / 170);
        const swing = attPower - defPower;
        const damage = Math.max(6, Math.min(26, 13 + swing * 14 + (Math.random() - 0.5) * 5));
        const recoil = Math.max(2, Math.min(9, 4 + defPower * 2.4 + Math.random() * 2.2));

        toState.control -= damage;
        fromState.control = Math.max(24, fromState.control - recoil);
        fromState.supply = Math.max(18, fromState.supply - (3 + Math.random() * 2));
        toState.troops = Math.max(1, (Number(toState.troops) || 0) - Math.max(1, Math.round(committed * (0.35 + Math.random() * 0.25))));

        const fromPos = getRegionScreenPos(fromDef);
        const toPos = getRegionScreenPos(toDef);
        if (fromPos && toPos) {
            war.attackFx.push({
                fromX: fromPos.x,
                fromY: fromPos.y,
                toX: toPos.x,
                toY: toPos.y,
                attacker: attackerFaction,
                startAt: performance.now(),
                duration: 460,
                committed
            });
            if (war.attackFx.length > 14) war.attackFx.splice(0, war.attackFx.length - 14);
        }

        if (toState.control <= 0) {
            toState.owner = attackerFaction;
            toState.control = 34 + Math.random() * 18;
            toState.supply = 42 + Math.random() * 20;
            toState.troops = Math.max(2, Math.floor(committed * 0.6));
            if (window.game && attackerFaction === war.playerFaction) {
                window.game.ore = Math.max(0, Number(window.game.ore) || 0) + 180;
                window.game.tech = Math.max(0, Number(window.game.tech) || 0) + 70;
                if (typeof window.updateUI === 'function') window.updateUI();
            }
        }
        return true;
    }

    function findCardBattleSourceRegion(war) {
        if (!war) return null;
        // Legacy tactical constraints removed: pick any owned region as launch source.
        const owned = LEVEL05_REGION_LAYOUT.find(def => {
            const s = war.regions[def.id];
            return !!s && s.owner === war.playerFaction;
        });
        return owned || null;
    }

    function resolveMove(war, faction, fromDef, toDef) {
        if (!war || !fromDef || !toDef) return false;
        const fromState = war.regions[fromDef.id];
        const toState = war.regions[toDef.id];
        if (!fromState || !toState) return false;
        if (fromState.owner !== faction || toState.owner !== faction) return false;
        if (!Array.isArray(fromDef.adj) || !fromDef.adj.includes(toDef.id)) return false;
        if ((Number(fromState.ap) || 0) <= 0) return false;
        if ((Number(fromState.troops) || 0) < 2) return false;

        const moved = Math.max(1, Math.min(7, Math.floor((Number(fromState.troops) || 0) * 0.45)));
        fromState.troops = Math.max(1, (Number(fromState.troops) || 0) - moved);
        toState.troops = Math.max(1, Math.min(40, (Number(toState.troops) || 0) + moved));
        fromState.ap = Math.max(0, (Number(fromState.ap) || 0) - 1);

        const fromPos = getRegionScreenPos(fromDef);
        const toPos = getRegionScreenPos(toDef);
        if (fromPos && toPos) {
            war.attackFx.push({
                fromX: fromPos.x,
                fromY: fromPos.y,
                toX: toPos.x,
                toY: toPos.y,
                attacker: faction,
                startAt: performance.now(),
                duration: 380,
                committed: moved,
                moveOnly: true
            });
            if (war.attackFx.length > 14) war.attackFx.splice(0, war.attackFx.length - 14);
        }
        return true;
    }

    function runAiTurn(war, faction) {
        if (!war || faction === war.playerFaction) return;
        for (let step = 0; step < 2; step++) {
            const candidates = [];
            LEVEL05_REGION_LAYOUT.forEach(def => {
                const state = war.regions[def.id];
                if (!state || state.owner !== faction) return;
                if ((Number(state.ap) || 0) <= 0 || (Number(state.troops) || 0) < 2) return;
                (def.adj || []).forEach(adjId => {
                    const targetDef = LEVEL05_REGION_LAYOUT.find(r => r.id === adjId);
                    const targetState = targetDef ? war.regions[targetDef.id] : null;
                    if (!targetDef || !targetState || targetState.owner === faction) return;
                    const value = (100 - targetState.control) + (targetState.owner === war.playerFaction ? 16 : 0) + Math.random() * 12;
                    candidates.push({ fromDef: def, toDef: targetDef, score: value });
                });
            });
            if (!candidates.length) break;
            candidates.sort((a, b) => b.score - a.score);
            resolveAttack(war, faction, candidates[0].fromDef, candidates[0].toDef);
        }
    }

    function finishRound(war) {
        if (!war) return;
        war.turn = Math.max(1, Number(war.turn) || 1) + 1;
        LEVEL05_FACTION_ORDER.forEach(f => runAiTurn(war, f));
        Object.keys(war.regions).forEach(id => {
            const state = war.regions[id];
            state.control = Math.max(8, Math.min(100, state.control + 1.5 + Math.random() * 2.2));
            state.supply = Math.max(10, Math.min(100, state.supply + 2.2 + Math.random() * 3));
            if (state.owner === 'none') {
                state.troops = 0;
            } else {
                const isMinor = isMinorFaction(state.owner);
                const growth = isMinor ? (Math.random() < 0.35 ? 1 : 0) : (1 + (Math.random() < 0.35 ? 1 : 0));
                const cap = isMinor ? 9 : 22;
                state.troops = Math.max(1, Math.min(cap, (Number(state.troops) || 0) + growth));
            }
        });
        refreshTurnAp(war);
        war.phase = 'player';
        war.selectedRegionId = null;
        checkElimination(war);
    }

    function tryEndPlayerTurn(war) {
        // Legacy turn-based war flow is disabled in card-battle mode.
        // Keep function for compatibility with existing click path.
        if (!war) return;
    }

    function handleMapClick(x, y) {
        const war = ensureWarState();
        if (!war) return false;
        if (war.phase === 'gameover') return false;

        const now = performance.now();
        if (now - (war.lastActionAt || 0) < 120) return true;
        war.lastActionAt = now;
        war.phase = 'player';

        let hit = getRegionByPoint(x, y);
        if (!hit) hit = getNearestRegionByPoint(x, y);
        if (!hit) {
            war.selectedRegionId = null;
            return isPointInMap(x, y);
        }

        const hitState = war.regions[hit.id];
        if (!hitState) return true;
        // Disable old region selection behavior in card-battle mode.
        if (hitState.owner === war.playerFaction) {
            war.selectedRegionId = null;
            return true;
        }

        // Level 0.5 now enters card battle directly when clicking enemy/neutral territory.
        if (!window.CardBattle || window.CardBattle.isActive()) return true;

        const targetDef = hit;
        const sourceDef = findCardBattleSourceRegion(war);
        if (!sourceDef) return true;

        const fromId = sourceDef.id;
        const targetId = targetDef.id;
        const pFaction = getPlayerFactionFromGameCountry(war.playerFaction);

        window.CardBattle.start(pFaction, hitState.owner || 'none', {
            onVictory: () => {
                const fS = war.regions[fromId];
                const tS = war.regions[targetId];
                if (!fS || !tS) return;
                const committed = Math.max(2, Math.min(8, Math.floor((Number(fS.troops) || 0) * 0.35)));
                fS.troops = Math.max(1, (Number(fS.troops) || 0) - committed);
                tS.owner = war.playerFaction;
                tS.control = 40 + Math.random() * 20;
                tS.supply = 45 + Math.random() * 20;
                tS.troops = Math.max(2, Math.floor(committed * 0.6));
                if (window.game) {
                    window.game.ore = Math.max(0, Number(window.game.ore) || 0) + 220;
                    window.game.tech = Math.max(0, Number(window.game.tech) || 0) + 90;
                    if (typeof window.updateUI === 'function') window.updateUI();
                }
                checkElimination(war);
            },
            onDefeat: () => {
                const fS = war.regions[fromId];
                if (!fS) return;
                fS.troops = Math.max(1, Math.floor((Number(fS.troops) || 0) * 0.55));
                if (window.game) {
                    window.game.tech = Math.max(0, (Number(window.game.tech) || 0) - 40);
                    if (typeof window.updateUI === 'function') window.updateUI();
                }
            }
        });
        war.selectedRegionId = null;
        return true;
    }

    function handlePointerMove(x, y) {
        const war = ensureWarState();
        if (!war) {
            return { cursor: 'default' };
        }

        let hoverEndTurn = false;
        if (endTurnRect && war.phase === 'player') {
            const b = endTurnRect;
            const hitPad = 14;
            hoverEndTurn = x >= b.x - hitPad && x <= b.x + b.w + hitPad
                && y >= b.y - hitPad && y <= b.y + b.h + hitPad;
        }
        war.hoverEndTurn = hoverEndTurn;

        if (war.phase === 'player') {
            const hit = getRegionByPoint(x, y);
            war.hoverRegionId = hit ? hit.id : null;
        } else {
            war.hoverRegionId = null;
        }

        return {
            cursor: hoverEndTurn ? 'pointer' : 'default'
        };
    }

    function drawFactionFlagBadge(ctx, faction, x, y, size) {
        const w = Math.max(14, size * 0.95);
        const h = Math.max(10, size * 0.62);
        const left = x - w * 0.5;
        const top = y - h * 0.5;

        ctx.save();
        ctx.beginPath();
        ctx.roundRect(left, top, w, h, 2);
        ctx.clip();

        function drawStar(cx, cy, outerR, innerR, points = 5) {
            ctx.beginPath();
            for (let i = 0; i < points * 2; i++) {
                const ang = -Math.PI / 2 + (i * Math.PI) / points;
                const rr = i % 2 === 0 ? outerR : innerR;
                const px = cx + Math.cos(ang) * rr;
                const py = cy + Math.sin(ang) * rr;
                if (i === 0) ctx.moveTo(px, py);
                else ctx.lineTo(px, py);
            }
            ctx.closePath();
            ctx.fill();
        }

        if (faction === 'ru') {
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top, w, h / 3);
            ctx.fillStyle = '#2563eb';
            ctx.fillRect(left, top + h / 3, w, h / 3);
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top + (h * 2) / 3, w, h / 3);
        } else if (faction === 'cn') {
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#facc15';
            drawStar(
                left + w * 0.28,
                top + h * 0.36,
                Math.max(2, h * 0.18),
                Math.max(1, h * 0.075),
                5
            );
            const smallOuter = Math.max(0.9, h * 0.06);
            const smallInner = Math.max(0.45, h * 0.026);
            const smallStars = [
                [0.43, 0.18],
                [0.50, 0.29],
                [0.50, 0.44],
                [0.42, 0.56]
            ];
            smallStars.forEach(s => {
                drawStar(
                    left + w * s[0],
                    top + h * s[1],
                    smallOuter,
                    smallInner,
                    5
                );
            });
        } else if (faction === 'eu') {
            ctx.fillStyle = '#1d4ed8';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#facc15';
            for (let i = 0; i < 8; i++) {
                const a = (i / 8) * Math.PI * 2;
                const sx = left + w * 0.5 + Math.cos(a) * w * 0.17;
                const sy = top + h * 0.5 + Math.sin(a) * h * 0.22;
                ctx.beginPath();
                ctx.arc(sx, sy, Math.max(1, h * 0.04), 0, Math.PI * 2);
                ctx.fill();
            }
        } else if (faction === 'jp') {
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#dc2626';
            ctx.beginPath();
            ctx.arc(left + w * 0.5, top + h * 0.5, Math.max(2, h * 0.24), 0, Math.PI * 2);
            ctx.fill();
        } else if (faction === 'in') {
            ctx.fillStyle = '#f59e0b';
            ctx.fillRect(left, top, w, h / 3);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top + h / 3, w, h / 3);
            ctx.fillStyle = '#16a34a';
            ctx.fillRect(left, top + (h * 2) / 3, w, h / 3);
            ctx.fillStyle = '#2563eb';
            ctx.beginPath();
            ctx.arc(left + w * 0.5, top + h * 0.5, Math.max(1.1, h * 0.10), 0, Math.PI * 2);
            ctx.fill();
        } else if (faction === 'ir') {
            ctx.fillStyle = '#16a34a';
            ctx.fillRect(left, top, w, h / 3);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top + h / 3, w, h / 3);
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top + (h * 2) / 3, w, h / 3);
        } else if (faction === 'tw') {
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#1d4ed8';
            ctx.fillRect(left, top, w * 0.48, h * 0.58);
            ctx.fillStyle = '#ffffff';
            ctx.beginPath();
            ctx.arc(left + w * 0.24, top + h * 0.30, Math.max(1.1, h * 0.10), 0, Math.PI * 2);
            ctx.fill();
        } else if (faction === 've') {
            ctx.fillStyle = '#facc15';
            ctx.fillRect(left, top, w, h / 3);
            ctx.fillStyle = '#2563eb';
            ctx.fillRect(left, top + h / 3, w, h / 3);
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top + (h * 2) / 3, w, h / 3);
        } else if (faction === 'ua') {
            ctx.fillStyle = '#2563eb';
            ctx.fillRect(left, top, w, h / 2);
            ctx.fillStyle = '#facc15';
            ctx.fillRect(left, top + h / 2, w, h / 2);
        } else if (faction === 'br') {
            ctx.fillStyle = '#16a34a';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#facc15';
            ctx.beginPath();
            ctx.moveTo(left + w * 0.5, top + h * 0.14);
            ctx.lineTo(left + w * 0.84, top + h * 0.5);
            ctx.lineTo(left + w * 0.5, top + h * 0.86);
            ctx.lineTo(left + w * 0.16, top + h * 0.5);
            ctx.closePath();
            ctx.fill();
            ctx.fillStyle = '#1d4ed8';
            ctx.beginPath();
            ctx.arc(left + w * 0.5, top + h * 0.5, Math.max(1.1, h * 0.12), 0, Math.PI * 2);
            ctx.fill();
        } else if (faction === 'eg') {
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top, w, h / 3);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top + h / 3, w, h / 3);
            ctx.fillStyle = '#111827';
            ctx.fillRect(left, top + (h * 2) / 3, w, h / 3);
        } else if (faction === 'sa') {
            ctx.fillStyle = '#16a34a';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left + w * 0.20, top + h * 0.66, w * 0.60, Math.max(1, h * 0.07));
        } else if (faction === 'mx') {
            ctx.fillStyle = '#16a34a';
            ctx.fillRect(left, top, w / 3, h);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left + w / 3, top, w / 3, h);
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left + (w * 2) / 3, top, w / 3, h);
            ctx.fillStyle = '#b45309';
            ctx.beginPath();
            ctx.arc(left + w * 0.5, top + h * 0.52, Math.max(0.8, h * 0.06), 0, Math.PI * 2);
            ctx.fill();
        } else if (faction === 'ca') {
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top, w * 0.25, h);
            ctx.fillRect(left + w * 0.75, top, w * 0.25, h);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left + w * 0.25, top, w * 0.5, h);
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left + w * 0.46, top + h * 0.30, w * 0.08, h * 0.40);
        } else if (faction === 'pa') {
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top, w * 0.5, h * 0.5);
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left + w * 0.5, top, w * 0.5, h * 0.5);
            ctx.fillStyle = '#2563eb';
            ctx.fillRect(left, top + h * 0.5, w * 0.5, h * 0.5);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left + w * 0.5, top + h * 0.5, w * 0.5, h * 0.5);
        } else if (faction === 'il') {
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#2563eb';
            ctx.fillRect(left, top + h * 0.12, w, h * 0.14);
            ctx.fillRect(left, top + h * 0.74, w, h * 0.14);
            ctx.beginPath();
            ctx.moveTo(left + w * 0.5, top + h * 0.34);
            ctx.lineTo(left + w * 0.60, top + h * 0.56);
            ctx.lineTo(left + w * 0.40, top + h * 0.56);
            ctx.closePath();
            ctx.fill();
            ctx.beginPath();
            ctx.moveTo(left + w * 0.5, top + h * 0.66);
            ctx.lineTo(left + w * 0.60, top + h * 0.44);
            ctx.lineTo(left + w * 0.40, top + h * 0.44);
            ctx.closePath();
            ctx.fill();
        } else if (faction === 'iq') {
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top, w, h / 3);
            ctx.fillStyle = '#ffffff';
            ctx.fillRect(left, top + h / 3, w, h / 3);
            ctx.fillStyle = '#111827';
            ctx.fillRect(left, top + (h * 2) / 3, w, h / 3);
            ctx.fillStyle = '#16a34a';
            ctx.fillRect(left + w * 0.42, top + h * 0.42, w * 0.16, h * 0.14);
        } else {
            ctx.fillStyle = '#dc2626';
            ctx.fillRect(left, top, w, h);
            ctx.fillStyle = '#ffffff';
            for (let i = 1; i <= 3; i++) {
                ctx.fillRect(left, top + (h * i) / 7, w, Math.max(1, h * 0.08));
            }
            ctx.fillStyle = '#1d4ed8';
            ctx.fillRect(left, top, w * 0.42, h * 0.56);
        }

        ctx.restore();
        ctx.save();
        ctx.lineWidth = 1;
        ctx.strokeStyle = 'rgba(255,255,255,0.9)';
        ctx.strokeRect(left, top, w, h);
        ctx.restore();
    }

    function drawWarOverlay(ctx, panelX, panelY, panelW, panelH, imageRect) {
        const war = ensureWarState();
        if (!war) return;
        war.phase = 'player';
        if (imageRect && Number.isFinite(imageRect.x) && Number.isFinite(imageRect.y)
            && Number.isFinite(imageRect.w) && Number.isFinite(imageRect.h)
            && imageRect.w > 1 && imageRect.h > 1) {
            mapRect = { x: imageRect.x, y: imageRect.y, w: imageRect.w, h: imageRect.h };
        } else {
            mapRect = { x: panelX, y: panelY, w: panelW, h: panelH };
        }

        mapClipRect = { x: mapRect.x, y: mapRect.y, w: mapRect.w, h: mapRect.h };
        if ((Number(war.turn) || 0) <= 1) {
            refreshTurnAp(war);
        }

        endTurnRect = null;

        ctx.save();
        ctx.beginPath();
        ctx.rect(panelX, panelY, panelW, panelH);
        ctx.clip();
        LEVEL05_REGION_LAYOUT.forEach(def => {
            const state = war.regions[def.id];
            if (!state) return;
            const screenPoly = toScreenPoly(def);
            if (!screenPoly || screenPoly.length < 3) return;
            const pos = getRegionScreenPos(def);
            if (!pos) return;
            const px = pos.x;
            const py = pos.y;
            const rr = pos.r;
            const factionColor = LEVEL05_FACTION_COLOR[state.owner] || 'rgba(148,163,184,0.38)';
            const hovered = war.hoverRegionId === def.id;
            const isNeutral = state.owner === 'none';

            ctx.beginPath();
            ctx.moveTo(screenPoly[0].x, screenPoly[0].y);
            for (let i = 1; i < screenPoly.length; i++) {
                ctx.lineTo(screenPoly[i].x, screenPoly[i].y);
            }
            ctx.closePath();
            ctx.fillStyle = factionColor;
            ctx.fill();

            ctx.lineWidth = hovered ? 2.4 : 1.4;
            if (isNeutral) {
                ctx.setLineDash([7, 6]);
                ctx.strokeStyle = 'rgba(148,163,184,0.72)';
            } else {
                ctx.setLineDash([]);
                ctx.strokeStyle = hovered ? 'rgba(255,255,255,0.82)' : 'rgba(226,232,240,0.65)';
            }
            ctx.stroke();
            ctx.setLineDash([]);

            if (!isNeutral) {
                drawFactionFlagBadge(ctx, state.owner, px, py - rr * 0.04, rr * 0.95);
                const nameEn = getFactionNameEn(state.owner);
                if (nameEn) {
                    ctx.fillStyle = 'rgba(255,255,255,0.90)';
                    ctx.font = `700 ${Math.max(7, rr * 0.18)}px sans-serif`;
                    ctx.textAlign = 'center';
                    ctx.textBaseline = 'bottom';
                    ctx.fillText(nameEn, px, py - rr * 0.36);
                }
            } else {
                ctx.fillStyle = 'rgba(203,213,225,0.8)';
                ctx.font = `${Math.max(10, rr * 0.46)}px sans-serif`;
                ctx.textAlign = 'center';
                ctx.textBaseline = 'middle';
                ctx.fillText('[]', px, py - rr * 0.05);
            }

            // Legacy troop number display disabled in card-battle mode.
        });

        const now = performance.now();
        war.attackFx = (war.attackFx || []).filter(fx => now - fx.startAt <= fx.duration);
        war.attackFx.forEach(fx => {
            const p = Math.max(0, Math.min(1, (now - fx.startAt) / fx.duration));
            const x = fx.fromX + (fx.toX - fx.fromX) * p;
            const y = fx.fromY + (fx.toY - fx.fromY) * p;
            ctx.save();
            ctx.strokeStyle = 'rgba(255,255,255,0.55)';
            ctx.lineWidth = 1.3;
            ctx.beginPath();
            ctx.moveTo(fx.fromX, fx.fromY);
            ctx.lineTo(x, y);
            ctx.stroke();
            ctx.fillStyle = LEVEL05_FACTION_COLOR[fx.attacker] || 'rgba(255,255,255,0.65)';
            ctx.beginPath();
            ctx.arc(x, y, 3 + fx.committed * 0.16, 0, Math.PI * 2);
            ctx.fill();
            ctx.restore();
        });

        ctx.fillStyle = 'rgba(255,255,255,0.90)';
        ctx.font = '700 13px sans-serif';
        ctx.textAlign = 'left';
        ctx.textBaseline = 'top';
        ctx.fillText('Card mode: click any enemy or neutral region to start battle', panelX + 10, panelY + 8);
        ctx.restore();
    }

    window.Level05Scene = {
        RENDER_MODE,
        isRenderMode,
        resetFrameState,
        ensureWarState,
        getRegionByPoint,
        isPointInMap,
        handleMapClick,
        handlePointerMove,
        drawWarOverlay
    };

    tryLoadGeoJsonLayout();
})();

